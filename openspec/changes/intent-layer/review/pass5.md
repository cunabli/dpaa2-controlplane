# Pass 5 — Adapters and shell: config/mc/tools (`intent-layer`, 648f804..4272019)

## Headline answers to the mandate's three questions

**(a) Structure:** keep-flat for all three crates (table below). No grouping proposal survives its own justification column.

**(b) parse.rs's 1,481 lines:** not migration residue. The file is 607 lines of code (lines 1–607) + 874 lines of in-module unit tests (608–1481). The code half is one converter per construct family plus the 6.3 rename gate; the 3.3b/c/d migrations *shrank* it — every deleted hand-rolled duplicate check is memorialized by a test asserting the TOML key-redefinition error replaced it (`parse.rs:966`, `1116`, `1173`). The two surviving hand-rolled identity checks (cross-family collision at `parse.rs:229-244`, reserved-`kernel` at `171-179`) are each provably unreachable by the keyed-table format and carry comments saying exactly why. The brief's "`[[tenant]]`-family hit in schema.rs" does not exist: every `[[…]]` hit in dpaa2-config is `[[crypto]]`, which stays an ordered array by ADR-0015 decision 4 (declaration order numbers the dpseci) — current, not a straggler.

**(c) Runtime checks at the trust boundary:** inventoried below. The interesting rows are the checks spelled on *both* sides of the config→api seam — those are deliberate (dpaa2-verify's raw-conformance suite pins the config-side spellings at `raw_itf.rs:272-275`), but only one of them documents its twin.

**V13 confirmation:** no fixed-tag residue in dpaa2-mc or dpaa2-tools. `dpaa2ctl` appears only as the binary name; ownership everywhere is declared-name-relative (`restool.rs:367-373`, `inventory.rs:130-155`). render.rs/status.rs and all three insta snapshots speak post-2.6a/post-6.6 vocabulary (`label=wan0`, `ConstructName`, dataplane never "regime"). No stale snapshots.

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification |
|----|-----------|----------|---------------|----------|-------------|--------------|
| PASS5-F1 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-mc/src/restool.rs:406-418` **and** `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/inventory.rs:147-154` | dup-cross | n/a | carries-cost | fold | The empty-label⇒`Foreign("dpl")` judgment is spelled twice, once per seam side — and the mc side does it directly under the comment "The backend reports, it never judges" (`restool.rs:367`). For dpmacs the *adapter* pre-judges availability (label→owner, `"dpl"` sentinel minted at `:413`); for every other family the *api* judges (`availability_of`). Fold: mc reports the anchoring dpni's raw label in `DpmacOffer` (or a shared `dpaa2_api` helper/`DPL_OWNER` const) and `availability_of` derives dpmac availability like the rest. Readability cost: low — one enum field swap, judgment lives once. `grep -rn '"dpl"' crates/*/src` → dpaa2-api only |
| PASS5-F2 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-mc/src/restool.rs:469` (`--num-queues={self.queues}`, `:119` queues=nproc) vs `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/derive.rs:505` and `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/tests/snapshots/render__dry_run_reference.snap:329` (`num_queues=5`) | seam-leak | n/a | follow-up bead | Two dpni-sizing authorities: compile derives `num_queues` from T (`if is_kernel { cpus } else { t }`) and prints it in the dry-run, but `Transition::Create { port, label }` drops the attribute and `RestoolMc` re-derives from host nproc (`ls-addni` mirror). For a userspace port the dry-run shows `num_queues=5`, `ensure` creates 16 — and `render.rs:19-20`'s "This is the exact plan `ensure` executes" over-claims for the object section. Bead: carry `Attributes::Dpni` across `McControl::create_dpni` (sibling of gqf.57's unanchored-family executor); meanwhile amend the render.rs claim. Verify: shim test asserting `--num-queues` equals the compiled attribute |
| PASS5-F3 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-config/src/parse.rs:369-378,440-443,466-470,496-499,522-543` **twinned with** `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/refuse.rs:760-767` (+ `TenantAbsent`/`MemberUnresolved` variants) | dup-cross | n/a | amend | The pool/restricted, tenant-reference, and fabric-member checks are deliberately duplicated across the seam (parse refuses TOML input with named errors; compile re-refuses programmatic Intents), and `raw_itf.rs:272-275` **pins the parse-side error spellings** for model conformance — yet only `parse_family` (`parse.rs:582-585`) documents the split. A well-meaning dedup of any other pair breaks raw-conformance silently. Fix: one-line "compile re-refuses this as `<Variant>`; raw_itf pins this wording" note per duplicated site. Verify: `grep -n "raw_itf\|compile refuses" crates/dpaa2-config/src/parse.rs` covers all five sites |
| PASS5-F4 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/intent.rs` (`Tenant.pool: TenantName`, `""` sentinel); enforcement scattered at `parse.rs:368-378`, `refuse.rs:760-767,793`, `derive.rs:500` (`is_restricted = !c.pool.is_empty()`), `compiled.rs:482`, test boilerplate `dpaa2-tools/tests/render.rs:74` | typestate-gap | n/a | new ADR note + follow-up bead | Option-as-empty-string: `Isolation::Restricted` with no pool (and its inverse) are representable, forcing the F3 check pair, and `derive.rs:500` re-derives restrictedness from the sentinel rather than the isolation field — two sources of the same truth. Candidate: `Isolation::Restricted { pool: TenantName }` deletes both compile-side refusals and the sentinel. Blast radius is cross-crate (qnt model `types.qnt`, raw_itf mapping, refusal alphabet), so this is a coordinated change, not a cleanup — the parse-side *named error* stays regardless (boundary check, per mandate caution). Verify: `grep -rn 'pool.is_empty\|pool: "".into' crates/` → empty after |
| PASS5-F5 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/src/link.rs:8` and `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/tests/link_gen.rs:1` | doc-drift | intent-layer phase 6 minting its own 6.3/6.4 (155569d..6175a97) | misleads-a-reader | amend | "(task 6.4)" / "(tasks 6.3/6.4)" here mean the archived **2026-07-04 add-dpaa2-provisioning** tasks (`.link` generation, fixed-link), but the same crate's neighbours cite intent-layer's 6.3 (rename clause: `schema.rs:75`, `parse.rs:184`) — three changes now own a "task 6.3" and a reader following link.rs's citation lands on the edit-alphabet task. Amend: qualify with the change slug. Verify: `grep -rn "task 6\.[0-9]" crates/ | grep -v "intent-layer\|provisioning\|restool-baseline"` → empty |
| PASS5-F6 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-config/src/parse.rs:547-551` | copy-drift | n/a | amend | Crypto refusal subjects are bare `` `[[crypto]]` `` — the only construct errors that cannot name the offending block, contradicting `schema.rs:16-19`'s own rationale ("so `crate::parse` can name the offending construct"). Include the 1-based block index (its dpseci ordinal, decision 4 — doubly useful). Verify: `parse_err` test asserting the index appears |
| PASS5-F7 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/src/status.rs:18,44` | typestate-gap | n/a | amend | `PortStatus.name: String` flattens `DesiredPort.name: ConstructName` (`model.rs:268`) back to a bare `String` in a public lib struct — the exact regression pattern the account rule guards ("name slots use dpaa2-api newtypes, never String", caught 2026-09-03). Type it `ConstructName`; Display at the print site. Verify: `grep -rn "name: String" crates/dpaa2-tools/src` → empty |
| PASS5-F8 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/tests/render.rs:78` and `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/src/main.rs:292,297` | copy-drift | task 2.3a (fa1d75c) | carries-cost | fold (into PASS1-F7's sweep) | Test-helper params named `owner` for the port's `tenant` field (V3-retired sense) — two more sites for pass 1's `\bowner\b` sweep; leave-it acceptable if F7's sweep scopes to models/derive only. Same grep as PASS1-F7 |
| PASS5-F9 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/src/main.rs:249-277` (`compile_intent`, `complete_kernel`) vs `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-tools/src/lib.rs:6` ("the `dpaa2ctl` binary is a thin CLI over it") | structure | n/a | leave-it (fold when a second frontend lands) | The shared read→complete→compile pipeline of three subcommands — including `complete_kernel`, a *pure* intent transform with its own unit tests — lives in the binary, not the lib, so no integration test or future frontend (TUI is in scope per the repo README) can reuse it, and the lib's "thin CLI" claim is mildly false. One-file fold into `engine.rs` when a second consumer exists; today it is one consumer, so churn to move pre-emptively. Verify (when folded): `grep -n "fn complete_kernel" crates/dpaa2-tools/src/main.rs` → empty |

## Runtime-check inventory (config→api trust boundary + mc observation boundary)

| invariant | where checked | type-enforced? | typestate candidate? | blast radius |
|---|---|---|---|---|
| schema version = 1 | parse.rs:79-96 | no | boundary-exempt | local |
| declared name is a valid iface name, never `family.N` (d13) | parse.rs:149-166 (`validate()` on newtype) | witness-exists (newtype has `validate`; `From<String>` stays infallible on purpose — raw board labels must be representable) | boundary-exempt | cross-crate |
| duplicate name within a family | TOML key redefinition (schema.rs `keyed`) | **yes — format-enforced (3.3c/d)** | n/a (done) | local |
| cross-family construct collision | parse.rs:229-244 | no (TOML's separate tables can't share a key namespace) | boundary-exempt | local |
| reserved `kernel` never declared | parse.rs:172-179 | no; **one-sided** — no compile refusal variant for a programmatic Intent declaring `kernel` | no (compile tolerates it via `effective_tenants`) | local |
| count keys / dpni pinning rejected by name | schema.rs:101-128 + parse.rs:399-419 | witness-exists (deliberately representable to name the error, design D1) | boundary-exempt | local |
| dpmac/mac well-formed | parse.rs:425-433 | no (String on purpose, schema.rs:16-19) | boundary-exempt | local |
| tenant references resolve | parse.rs:440,466,496,548,569 **+** refuse.rs `TenantAbsent` | no | boundary-exempt — deliberate dup, raw_itf-pinned (F3) | cross-crate |
| fabric member resolves | parse.rs:522-543 **+** refuse.rs `MemberUnresolved` | no | boundary-exempt — deliberate dup (F3) | cross-crate |
| link ends distinct | parse.rs:472-476 **only** — no compile-side variant | no; **one-sided** | open question: a programmatic self-loop link reaches `derive.rs:1036` unrefused | cross-crate |
| restricted ⇔ pool present | parse.rs:369-378 **+** refuse.rs:760-767 | no — `""` sentinel (F4) | **yes** — `Isolation::Restricted { pool }` (F4); parse-side named error stays | cross-crate |
| extra family known / companion / positive | parse.rs:586-592 (name) vs refuse.rs `ExtraNotCompanion`/`ExtraNotPositive` (semantics) | no | boundary-exempt — the one *documented* split; the exemplar F3 asks the others to copy | cross-crate |
| rename rule (i) — no double claim | parse.rs:312-356 only | no; one-sided (TOML boundary; matcher consumes accepted clauses) | open question for programmatic Intents | cross-crate |
| dpmac offer attrs mandatory | restool.rs:393-399 | no | boundary-exempt (parse failure, never a default — correct) | local |
| empty label ⇒ DPL resident | restool.rs:406-418 **+** inventory.rs:147-154 | no | fold, not typestate (F1) | cross-crate |

## Structure verdicts

| crate | verdict | proposed layout | files moved | what grouping would communicate that flat doesn't |
|---|---|---|---|---|
| dpaa2-config | **keep-flat** | — | 0 | Nothing — `schema` (raw serde shapes) / `parse` (validate+convert) *is* the D10 seam, stated in both module docs; a third module would blur it. parse.rs's bulk is unit tests, which Rust idiom keeps in-module |
| dpaa2-mc | **keep-flat** | — | 0 | Nothing — restool (policy) / parse (pure) / runner (seam) / kernel (sysfs) is one module per concern; lib.rs:9-14 already narrates the map |
| dpaa2-tools | **keep-flat** | — | 0 | Nothing worth a move today; the only boundary wrinkle is F9 (pipeline logic in the binary), which is a one-file fold *into the existing flat layout* when a second frontend exists, not a grouping |

## Guideline nominations (≤5)

1. **Name slots use dpaa2-api newtypes, never `String`; convert only at the serde/display boundary.** Anchor: user-caught regression 2026-09-03 + PASS5-F7 (`status.rs:18`).
2. **A check duplicated across the config→api seam names its twin in a doc note (the `parse_family` pattern), because raw-conformance pins the config-side error spellings.** Anchor: `raw_itf.rs:272-275` + three undocumented dup sites (PASS5-F3).
3. **Task-number citations in code comments carry their change slug.** Anchor: three changes now own a "task 6.3/6.4" and adjacent files cite different ones (PASS5-F5; `schema.rs:75` vs `link.rs:8`).
4. **An adapter that declares "reports, never judges" performs no classification; judgment constants and rules live once, core-side.** Anchor: `restool.rs:367` comment vs `restool.rs:406-418` behavior, twin at `inventory.rs:147-154` (PASS5-F1).

## Fixed footer

**What was read (full):** all 14 in-scope files — `crates/dpaa2-config/src/{lib,schema,parse}.rs`, `crates/dpaa2-config/tests/doc_examples.rs`, `crates/dpaa2-mc/src/{lib,restool,parse,runner,kernel}.rs`, `crates/dpaa2-mc/tests/shim.rs`, `crates/dpaa2-tools/src/{lib,main,engine,render,status,link}.rs`, `crates/dpaa2-tools/tests/{render,convergence,link_gen}.rs`, all three insta snapshots. Cross-seam verification reads: `crates/dpaa2-api/src/inventory.rs` (full), `model.rs:360-418` + `DesiredPort`, targeted greps over `refuse.rs` (variant list, pool checks), `derive.rs` (`num_queues`), `raw_itf.rs` (error-string pinning), `intent_pairing.rs` (kernel completion), archive `tasks.md` task-number resolution, `pass1.md` (full).

**Deliberately not read:** `dpaa2-api` compile/derive bodies beyond the seam checks above (Passes 2–4 territory); `dpaa2-verify` suites beyond raw_itf/fitcheck grep hits; `dpaa2-hal` (untouched by the mandate); `models/**` qnt beyond pass 1's classifications; `crates/dpaa2-mc/tests/fixtures/*` bodies (shapes asserted via shim.rs).

**Open questions:**
1. **Self-loop link and contradictory-rename from a *programmatic* Intent** (inventory rows "link ends distinct", "rename rule (i)"): both are parse-only checks with no compile-side twin. Whether `compile` behaves sanely on such Intents is unverified here — a Pass covering derive.rs should confirm, or the F3 doc-note work should record them as intentionally TOML-only.
2. **Is PASS5-F2's num-queues divergence already acknowledged in a bead?** gqf.57 covers unanchored families; the dpni port facet *is* executed, so the attribute gap looks unclaimed. Synthesis should check `bd` before filing a duplicate.
3. PASS5-F9's fold trigger (a second frontend) should be noted wherever the TUI work is eventually proposed, per the "follow-ups belong under the epic" convention.

**Token spend:** ~95k of the 140k budget (agent-reported); harness-measured ~179k total. Complete, no scope dropped.
