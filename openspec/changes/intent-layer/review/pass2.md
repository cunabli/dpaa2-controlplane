# Pass 2 — Model layer (`intent-layer`, 648f804..4272019)

## Answer to Pass 1's open question 1

**Yes — exactly one more identifier carries retired vocabulary:** `wInterruptDrawn` (V5, retired 2.6a) at `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/intent/alphabet.qnt:325`, body already reads `UserspaceEvent`. Unlike `companionCountsByRegime` it is **not** lint-pinned (R11 at `crates/dpaa2-verify/src/ledger.rs:1040` bijects only *refusal* witnesses; structure witnesses are unlinted), so the rename is a cheap 3-site edit (finding F7). On PASS1-F10 itself: the model already records the deliberate bridge — `models/intent/types.qnt:35-37` states "the core Regime (comp::Kernel / comp::PollMode) stays the internal pricing key" — which supports Pass 1's accept-and-note lean; the missing piece is only the one-line gloss in ADR-0013 §6.

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification |
|----|-----------|----------|---------------|----------|-------------|--------------|
| PASS2-F1 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/intent/match.qnt:58-69` | stale | tasks 6.5–6.7 (28fa8b7 closes the loop) | misleads-a-reader | amend | Header block "RED RUST TWINS THIS MODULE AWAITS … These laws are model-only today; no failing Rust test is written here" — the twins shipped: `crates/dpaa2-api/src/matcher.rs:242-560`, `crates/dpaa2-verify/tests/intent_identity_props.rs`, `crates/dpaa2-verify/tests/intent_edit_replay.rs`. `grep -n "AWAITS\|model-only today" models/intent/match.qnt` → empty after fix |
| PASS2-F2 | `.../models/intent/scenarios/{fabric,vfabric,router,vwire,reference}.qnt` + same-stem `.toml` (10 sites, e.g. `vwire.qnt:25`, `fabric.toml:6-7`) | stale | task 3.4 (intent_pairing landed; also `models/helpers/intent-pairing.py` at the typecheck rung, `package.json:3`) | misleads-a-reader | amend | Every scenario says "the pairing is by convention until dpaa2-verify's test (task 3.4)" — `crates/dpaa2-verify/tests/intent_pairing.rs:62-88` enforces qnt⇄toml stems, frozen traces, and plan equality today. `grep -rn "by convention until" models/intent/scenarios/` → empty |
| PASS2-F3 | `.../models/intent/types.qnt:122-125` (claim) vs `.../models/intent/derive.qnt:617-619` (`fullOrder`) | typestate-gap | n/a (gap, not staleness) | breaks-a-claim | amend + follow-up bead | types.qnt claims "tenants/ports/links/fabrics … the derivation sorts them by name (sortByRank) and no longer consumes their order" — **false for tenants**: no `sortByRank` touches `intent.tenants`; `fullOrder` folds `effectiveTenants` in declaration order, so the emission list is tenant-position-dependent. Rust emits tenants name-sorted (`crates/dpaa2-config/src/schema.rs:142` `BTreeMap`), so the same TOML with tenants declared out of name order yields model order ≠ Rust order; every scenario/alphabet happens to declare tenants alphabetically (`vwire.qnt:64-66` ns1<ns2<vpp; alphabet init kernel+c1+c2 is the lone unsorted case, never paired), so the pairing test cannot witness it. Fix: `sortByRank` the tenants in `fullOrder` (and `toIntent`'s set-fold emission, `intent_raw.qnt:315`). Verify: directed run `compile(tenants:[b,a]) == compile(tenants:[a,b])` fails before, passes after; `cargo test -p dpaa2-verify intent_pairing` stays green |
| PASS2-F4 | `.../models/intent/invariants.qnt:298-302` (+ `alphabet.qnt:259-262`) | typestate-gap | task 3.3d (08138a3) scoped it; 3.3c (6001bd9) retired tenant/fabric arrays | carries-cost | amend | `positionIndependence` permutes only ports and links; crypto's exemption is recorded (ADR-0015 d4) but tenants/fabrics are silently unpermuted — no law and no recorded unknown covers a `[tenant.<name>]`/`[fabric.<name>]` block reorder. After F3's sort, extend the witness permutation to tenants+fabrics (fabrics are already order-free: `hwOwnedFabrics`/`attachOrigins`/`wire*Origins` all sort; edges/prov are Sets/Maps). Verify: reversed-tenants clause added to `positionIndependence`; `pnpm model:simulate` green |
| PASS2-F5 | `.../models/intent/derive.qnt:55-58` (`labelOf`) | dead + stale | task 6.6 (1a560b0, V13) | breaks-a-claim | delete | `labelOf` has zero callers (`grep -rn "labelOf" .` → the def only) and its comment restates the **retired** rendering "the Rust frontend renders `<tenant>/<family>/<ordinal>` (task 3.1)" — contradicting the same file's 6.6-amended header (:23-27) and INTENT_I5 (`invariants.qnt:184-187`, label = construct name). Delete def+comment; `pnpm model:typecheck` green |
| PASS2-F6 | `.../models/intent/derive.qnt:22-38` | stale | tasks 1.3 (refusals shipped) and 6.6 (item (a)) | misleads-a-reader | amend | "Open items left for 1.3 (refusals) and 3.1 (Rust label rendering)" presents closed work as pending — items (b)–(e) are live refusals (`UnknownRateClass`, `UnsupportedEdge`, `PortTenantMismatch`) that `main.qnt` runs pin; "(c) … 1.3 may narrow it" never happened. Rewrite as resolved cross-references. `grep -n "Open items left for 1.3" models/intent/derive.qnt` → empty |
| PASS2-F7 | `.../models/intent/alphabet.qnt:325` (def), `:55` (count block); `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/package.json:6` | stale | task 2.6a (c9921f9, V5) | misleads-a-reader | amend | Witness id `wInterruptDrawn` keeps the retired "interrupt" naming for a body that selects `UserspaceEvent`. Not R11-pinned (refusal witnesses only), so rename to `wEventDrawn` at 3 sites + re-run `pnpm model:coverage` (only the renamed id's label changes, count 2075 = `wUnpricedDataplane`'s twin). `grep -rn "InterruptDrawn" .` → empty |
| PASS2-F8 | `.../models/intent/invariants.qnt:21-22` and `.../models/intent/alphabet.qnt:246-247` vs `alphabet.qnt:16-18`, `main.qnt:13-15` | copy-drift | task 2.6c (83f5579, added I9 to `compileInvariants`) | misleads-a-reader | amend | Two sites say the Apalache-marked subset is "I7/I8", two say "I7/I8/I9"; the code (`invariants.qnt:309-311` `compileInvariants`, `alphabet.qnt:248` `compileLaws`) is I7+I8+I9. COVERAGE.md marks only I7/I8 apalache-marked (`:201-202`) — a third spelling. Align all on I7/I8/I9. `grep -rn "INTENT_I7/I8[^/]" models/` → empty |
| PASS2-F9 | `.../models/intent/invariants.qnt:12`; `.../models/intent/main.qnt:10,701` | copy-drift | task 3.3d (08138a3, added I10) | carries-cost | amend | "The ids `INTENT_I1..I9` are this change's own" / "(ids INTENT_I1..I9)" / "compileInvariants (INTENT_I7 feasibility, INTENT_I8 companion counts)" — all pre-I10 (and pre-I9 for the last). ADR-0013:386 and COVERAGE.md:184 already say I1–I10. (`ledger.rs:2094`'s "I1–I9" is a lint-test *fixture string*, not a hit.) `grep -rn "I1..I9" models/intent/` → empty |
| PASS2-F10 | `.../models/intent/refuse.qnt:254` (rule 11) between `:238` (rule 8) and `:276` (rule 9) | structure | task 2.6e (b994ea6) inserted it in place | carries-cost | amend | The rule-numbering comments run 1,2,…,8,**11**,9,10,12 in file order — the one visible clause-by-clause-growth artifact of the 2.6a–e amendments (the defs themselves are clean). Renumber comments or drop numbers (the union at `:327-339` is the authoritative order). Verify: rule comments strictly increasing in file order |
| PASS2-F11 | `.../models/intent/observed.qnt:101` (`type Plan`) vs `.../models/intent/derive.qnt:106` (`type Plan`) | structure | n/a | carries-cost | amend | Two unrelated `Plan` types in one corpus. No module imports both flat *today*, but the natural follow-on law (relating a compiled Plan's objects to the match Plan's `CompiledObject`s — the Rust twin's cone closure, `observed.qnt:211-216`) forces exactly that import. Rename the match layer's to `MatchPlan` while it has 3 consumers (match.qnt, edits.qnt, edits_itf.rs's model twin). `grep -rn "type Plan" models/intent/` → one hit per distinct name |
| PASS2-F12 | `.../models/intent/intent_raw.qnt:7` | copy-drift | task 3.3d (08138a3) | carries-cost | amend | Header says "mirrors the post-3.3b/3.3c TOML surface"; the module body (`:17`, keyed ports/links) and COVERAGE.md:250 say post-3.3b/3.3c/**3.3d**. One-word fix. `grep -n "3.3b/3.3c TOML" models/intent/intent_raw.qnt` → empty |
| PASS2-F13 | `.../models/intent/refuse.qnt:86-105` (`tenantAbsentRefusals`) ↔ `.../models/intent/intent_raw.qnt:238-258` (`tenantRefRefusals`) | dup-cross | n/a | carries-cost | amend (note), else leave-it | The dangling-tenant law lives at both the parse and compile stages, each mirroring its own Rust seam (parse.rs / refuse.rs) — **fold mechanism: leave-it** (folding would couple the parse model to the compile model across the sans-io seam); readability cost: low. The one real divergence to record: `rawResolves` (`intent_raw.qnt:168-169`) accepts KERNEL for *every* reference while compile accepts KERNEL only at link ends (`refuse.qnt:91-96`; `derive.qnt:585-587`), so an ownerless port (`tenant=""` → kernel via `effTenant`) parses OK then refuses `TenantAbsent{tenant:"kernel"}` — a tenant name the operator never typed. Deliberate posture, undocumented UX; one sentence in intent_raw.qnt's DEVIATION block. Verify: the sentence exists; `rawDuplicateNameTrace` (whose ports carry `tenant:""`) still replays |
| PASS2-F14 | `.../models/intent/alphabet.qnt:51-63` ↔ `.../models/COVERAGE.md:206-244` (witness-count blocks) | duplicate | n/a | carries-cost | follow-up bead | Two hand-pasted copies of the seed-20260831 witness counts (a third partial copy is the `--witnesses` list in `package.json:6`). Fold mechanism: leave-it as dated snapshots, but ADR-0014 says a table restating a source of truth extends the lint **or files its bead** — no lint covers these counts (R11 covers witness *names* of refusal variants only). Readability cost of folding: low (point alphabet.qnt at COVERAGE.md). Verify: bead exists or alphabet.qnt block reduced to a pointer |

**Reviewed-clean (no rows):** all 23 committed traces are load-bearing — 9 enumerated by `intent_replay.rs:14-22` (5 reused by `intent_pairing.rs:36`), 11 by `raw_conformance.rs:21-31`, 3 by `intent_edit_replay.rs:41`; nothing orphaned, including `models/traces/retro*.itf.json` (retro_replay.rs, out of scope). The **two generations of surface+replay machinery are not duplicates**: replay.qnt freezes `compile`'s oracle, raw_replay.qnt freezes `parse`'s, the edits sweeps freeze `converge`'s — three distinct pure functions, three distinct Rust diffing tests; both generations are load-bearing. `intent_raw.qnt`'s refusal-clause/shape-predicate pairs (`duplicateNameRefusals`/`hasDuplicateConstruct` etc.) are deliberate independent derivations so an omission fails `nearMissRefusedByName` instead of hiding — not foldable. ADR-0013 §6 lists I1–I10 with names matching `invariants.qnt` (R12-linted at `ledger.rs:1108`, floor green). Addendum to PASS1-F7's `owner` sweep: `alphabet.qnt:180,203,218` bind `nondet owner` — three more sites for that sweep.

## Triplicate-shape catalog (for Pass 7)

| Shape | Quint def | Rust type | Verify lint site | Note |
|-------|-----------|-----------|------------------|------|
| Refusal vocabulary (24) | `models/intent/refuse.qnt:33` | `crates/dpaa2-api/src/refuse.rs:48` + `REFUSAL_VARIANTS` `:259` | `ledger.rs:1191` (⇄ ADR-0013 §5) + R11 `ledger.rs:1040` (⇄ alphabet witnesses) | deliberate, doubly lint-backed |
| Warning vocabulary | `refuse.qnt:66` | `refuse.rs:328` | **none found** (R11 parses `hasRefusal` matches only) | 2 variants; cheap to add to R11 or accept |
| Plan invariants I1–I10 | `invariants.qnt:26-311` | `crates/dpaa2-api/tests/compile_props.rs` (props) | R12 `ledger.rs:1108` (invariants.qnt ⇄ ADR-0013 §6 ⇄ COVERAGE.md) | deliberate, the R12 flagship |
| Canonical name order | `types.qnt:161` `NAME_RANK` | `str::cmp` via BTree collections (`derive.rs:140`) | `intent_pairing.rs` plan equality + `models/helpers/intent-pairing.py` | deliberate but hand-maintained: every new model name edits the 57-entry map; lint is indirect (pairing fails only if a scenario uses the name) |
| Family-name spellings (16) | `intent_raw.qnt:153` `FAMILY_NAMES` | `dpaa2-api` `Family` + `parse.rs` `parse_family` | `raw_conformance.rs` accepted-arm agreement | third copy of a list whose truth is `core/types.qnt`'s constructors — candidate accidental triplication |
| Raw refusal vocabulary | `intent_raw.qnt:132` `RawRefusal` | **no Rust enum** — `parse.rs` stringly `cfg()` errors | `raw_conformance.rs` per-variant error matcher (`raw_itf.rs`) | asymmetric: the matcher regex-binds strings to variants; drift shows as DEVIATION churn |
| Dataplane/Isolation/Switching | `types.qnt:38,53,86` | `intent.rs:44,60` (+Switching) | serde values in `schema.rs`; replay agreement | deliberate |
| Worker table (10G⇒2, 25G⇒5) | `derive.qnt:136` `WORKER_TABLE` | `derive.rs:~52` | `intent_replay.rs`/`intent_pairing.rs` plan equality | deliberate |
| Reference inventory | `inventory.qnt:14` (GENERATED) | snapshot inventory (`intent_pairing.rs:149`) | generation from `models/board/baselines/reference.json` replaces lint | deliberate — the only generated copy |
| Matcher + identity laws | `observed.qnt:62-217`, `match.qnt:203-259` | `matcher.rs:242-560` | `intent_identity_props.rs` + `intent_edit_replay.rs` (`edits_itf.rs`) | deliberate, MBT-backed |
| Scenario twins | `scenarios/*.qnt` | `scenarios/*.toml` (operator surface) | `intent_pairing.rs:62-88` stems + `ledger_lint.rs:106-139` + `ledger.rs:1156` (⇄ ADR-0013 §7) | deliberate |
| Companion draw | `core/companions.qnt:35` | sizing in `derive.rs` | `compile_props.rs` I8 prop + replay | deliberate; `Regime` stays ADR-0012 vocabulary by recorded bridge (`types.qnt:35-37`) |
| Coverage witness lists | `alphabet.qnt:288-330`, `raw_alphabet.qnt:169-190`, `edits.qnt:210-227` | — | `package.json:6` `--witnesses` + R11 (refusal subset only) | structure witnesses unlinted — F7's freedom and F14's risk |

---

## Fixed footer

**What was read (full):** `models/intent/{types,invariants,derive,refuse,alphabet,main,replay,raw_replay,intent_raw,raw_alphabet,observed,match,edits,inventory}.qnt`; `models/intent/scenarios/vwire.qnt`; `models/core/companions.qnt`; `package.json`; `openspec/changes/intent-layer/review/pass1.md`. **Targeted:** `models/COVERAGE.md` (intent rows), `docs/adr/0013` (§6 grep), `models/main.qnt` (intent grep), `crates/dpaa2-api/src/{derive.rs:555-595 + greps, refuse.rs greps, intent.rs greps, matcher.rs greps}`, `crates/dpaa2-config/src/{parse.rs:185-274, schema.rs greps}`, `crates/dpaa2-verify/{src/ledger.rs:830-1200,2080-2110 greps, tests/* trace-list greps}`. Tree-wide greps: `labelOf`, `InterruptDrawn`, `NAME_RANK`, `R12`, `by convention until`, trace filenames.

**Deliberately not read:** the other four scenario `.qnt` bodies beyond greps (vwire is the richest; the stale-3.4 phrase was swept tree-wide); `*.itf.json` trace contents (generated; replayer-verified); Rust bodies of `matcher.rs`/`derive.rs`/`compile_props.rs` beyond seam checks (Rust passes own them); `models/board/**`, `models/retro/**`, `models/families/*` beyond the dpsw/dpseci draw imports; ADR-0015 full text (pass 1 covered; decision numbers taken from the modules' own citations).

**Open questions for later passes:**
1. **F3/F4 are this pass's judgement call for synthesis.** The Rust surface is position-independent for tenants by `BTreeMap` construction; the model is not, and the claim in `types.qnt:122-125` is wrong as written. Cheapest repair is model-side (`sortByRank` tenants in `fullOrder` + widen I10); the alternative — scope the claim and record tenants/fabrics as an exemption like crypto's — leaves a real model/Rust divergence one unsorted scenario away. The Rust pass should confirm `derive.rs` emission order really is tenant-Vec order (it clones `intent.tenants`), i.e. that the divergence is model-vs-Rust, not Rust-internal.
2. **Pass 7 (triplication):** the two candidates that look accidental are the Family-name list (4th copy counting `core/types.qnt`) and the unlinted Warning pair; everything else is lint- or generation-backed per ADR-0014.
3. **F13's UX wrinkle** (`TenantAbsent{tenant:"kernel"}` for an ownerless port) may deserve a dedicated refusal variant rather than a doc note — that is a vocabulary change (ADR-0013 §5 + REFUSAL_VARIANTS + R11), so it belongs to a follow-up bead, not this review's amend set.

**Token spend:** ~118k of the 150k budget (agent-reported); harness-measured ~217k total. All mandated scope covered, nothing left unreviewed.
