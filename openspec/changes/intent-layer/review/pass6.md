# Pass 6 — Spec/docs alignment (`intent-layer`, 648f804..4272019)

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification |
|----|-----------|----------|---------------|----------|-------------|--------------|
| PASS6-F1 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/openspec/changes/intent-layer/specs/intent-compiler/spec.md:263-273` (+ `:128-129`); same phrase at `.../design.md:212`, `.../proposal.md:45`, `.../tasks.md:41` | stale | task 6.6 (1a560b0) + ADR-0015 decision 3's in-place supersession (label = bare construct name, decisions 9+13) | breaks-a-claim | amend **before archive** | The promotable delta still requires "its DPRC label SHALL be rendered from that key" and the scenario pins label `router/dpio/3` — shipped `compiled.rs:251-256` returns the owning construct's name (`label=router` in `render__dry_run_crypto_and_warning.snap:180`), and INTENT_I5/ADR-0015 d3 retired the `<tenant>/<family>/<ordinal>` rendering. Pass 1's V13 sweep missed it because no `dpaa2ctl` token appears. Archiving unamended promotes a retired law into `openspec/specs/`. `grep -rn 'router/dpio/3\|rendered from that key\|rendered from the key' openspec/changes/intent-layer/` → empty |
| PASS6-F2 | `.../openspec/changes/intent-layer/specs/intent-compiler/spec.md:92-93` | stale | task 3.2 (ce38382) | misleads-a-reader | amend before archive | Spec twin of PASS1-F11 (which fixed only design.md): "`compile(intent, inventory)` SHALL be a pure function returning `Result<Plan, Refusal>`" — shipped is `Result<Compiled, BTreeSet<Refusal>>` (`refuse.rs:876`), and `Plan` is reconcile's output type, a different thing. `grep -n 'Result<Plan, Refusal>' openspec/changes/intent-layer/specs/intent-compiler/spec.md` → empty |
| PASS6-F3 | `.../openspec/changes/intent-layer/specs/intent-compiler/spec.md:138-140` | stale | ADR-0013 §5 acceptance / task 3.0 (dadb249); `refuse.rs:50` "(DPDCEI-I1 generalised)" | misleads-a-reader | amend before archive | The refuses-by-name list opens with "tenant absence (a dpci or dpdcei intent with no userspace tenant named, DPDCEI-I1)" — no dpci/dpdcei construct exists in the shipped five-construct `Intent`; `TenantAbsent` is the generalised any-dangling-reference rule the same list restates later (":a construct naming an undeclared tenant, port or fabric"). Reword to "(DPDCEI-I1 generalised)" or drop the item. `grep -n 'dpci or dpdcei' openspec/changes/intent-layer/specs/intent-compiler/spec.md` → empty |
| PASS6-F4 | `.../openspec/changes/intent-layer/specs/intent-compiler/spec.md:136-162` | copy-drift | ADR-0013 §5 (24 accepted variants, R14-linted) | carries-cost | amend before archive | The SHALL-refuse enumeration maps to 23 of the 24 `REFUSAL_VARIANTS` (`refuse.rs:259-284`); `SelfMember` (a fabric member resolving to the fabric itself, `refuse.rs:65-68`) has no clause. Not scope creep — the ADR-0013 gate owns it — but the promoted spec must not under-enumerate its own SHALL list. Verification: every name in `REFUSAL_VARIANTS` maps to a clause in this requirement (same check PASS1-F12 imposes on design D5) |
| PASS6-F5 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/adr/0013-accepted-intent-vocabulary.md:509-517` | stale | tasks 5.1 (R11/R12/R14 landed in `ledger.rs`), 3.4 (`intent_pairing.rs`), 6.4 (R15) | misleads-a-reader | amend | §Consequences still says the enumerations "are slated for the `dpaa2-verify` ledger lint … in phase 3, tracked as bead gqf.34 … Until that lands, the models are the source of truth" — the lints and the pairing test shipped and are green. Rewrite as delivered cross-references (R11 §5, R12 §6, ledger R13-scenario/R14, pairing 3.4). `grep -n 'slated\|Until that lands' docs/adr/0013-*.md` → empty |
| PASS6-F6 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/adr/0014-linted-enumerations.md:84-88` | stale | task 3.3d (08138a3, I10) + task 5.1 (lint landed) | misleads-a-reader | amend | The ADR's own compliance exhibit reads "the INTENT_I1–I9 invariants (§6) … slated for the ledger lint in phase 3 under bead gqf.34" — ADR-0013 §6 lists I1–I10 (R12-linted) and the lint exists. The lint-law ADR carrying a drifted enumeration count is exactly its own 2.6b/2.6c precedent. Sibling of PASS2-F9/PASS3-F3; fix in the same sweep. `grep -n 'I1–I9\|slated' docs/adr/0014-*.md` → empty |
| PASS6-F7 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/COVERAGE.md:256-270` (raw-surface laws table) | doc-drift | n/a — unmet standing obligation (ADR-0014 rule 2: extend the lint **or name the bead in the table's own text, never in a follow-up**) | breaks-a-claim | follow-up bead (or extend the lint now) | The table restates `intent_raw.qnt`'s law names (`parseOkWellFormed`, `acceptedSurvives`, `nearMissRefusedByName`) — the exact class the identity-laws table got R15 for at task 6.4 (`ledger.rs:1285-1303`). The preamble says only "Not linted by R12" and names no bead; a model-side law rename drifts this table silently (raw_conformance replays traces, never parses COVERAGE). Cheapest fix is the R15 mechanism pointed at `intent_raw.qnt`. Verify: an R-rule cross-checks the table, or the preamble cites a bead id |
| PASS6-F8 | `.../openspec/changes/intent-layer/specs/reconciler/spec.md:86-99` | doc-drift | n/a — the MatchPlan→Transition lowering is unwired (PASS3-F14) | misleads-a-reader | fold into PASS3-F14's bead | Scenarios promise converge-level behavior ("the rename pass cross-binds them, the plan holds exactly two renames", "the converge changes nothing" on `Ambiguous`), but `grep -rn 'match_board\|MatchPlan' crates/dpaa2-tools/src` → 0; the converge path is reconcile-only. The requirement text itself is scoped to the `dpaa2-api` relation (met); the scenarios over-reach. The PASS3-F14 bead should own both the lowering and this scenario wording (or the scenarios are scoped to the relation at archive). Verify: bead text names `specs/reconciler/spec.md` scenarios |
| PASS6-F9 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/ROADMAP.md:23`; `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/adr/0005-intent-layer-network-constructs.md:3-5` | doc-drift | ADR-0014 minted 2026-09-02 inside the span; epic closed 2026-09-06 (b769f78) | carries-cost | amend (fold with PASS1-F14's date fix) | Row 3's Delivers omits ADR-0014 — a standing repo-wide law this epic minted (row 1 lists its ADRs 0002–0006 by convention). ADR-0005's status "amendment sealed 2026-09-05 with … `intent-layer` delivered" carries the same 09-05 date PASS1-F14 flags; the phase-6 deliverables and epic close are 2026-09-06. One coordinated date/deliverables amend. `grep -n 'ADR-0014' docs/ROADMAP.md` → non-empty |

## Dispositions of Pass 1's category-(b)/(c) hits and open questions 2–4

- **PASS1-F9 (ADR-0012) — pointer, not amendment.** ADR-0012 is a sealed, board-dated record in its own vocabulary; ADR-0013:71 licenses "`dataplane` selects the sizing regime" and `types.qnt:35-37` records the deliberate internal bridge (Pass 2's evidence). Rewriting the title/prose to `dataplane` would falsify a verified record and churn its filename across ADR-0013's eleven citations. Fix = two lines in ADR-0012's Supersedes/relates-to: "elaborated by ADR-0013 (the intent vocabulary spells the operator-facing selector `dataplane`); ADR-0015" — with the capture-ambivalence revisit trigger: if ADR-0012's draws ever change, ADR-0013 §4's pricing follows in the same change. Verify: `grep -n 'ADR-0013' docs/adr/0012-*.md` → non-empty.
- **PASS1-F10 (`companionCountsByRegime`) — accept-and-note**, same amend set as F9: one sentence in ADR-0013 §6 stating the id deliberately carries ADR-0012's pricing term (the model already says so at `types.qnt:35-37`). No 9-site rename; R12 lint untouched. Pass 2 confirmed the only other retired-vocabulary id (`wInterruptDrawn`) is unlinted and separately cheap (PASS2-F7).
- **PASS1-F15 (object-model.md) — amend, one bridge note only.** Add a head-of-file pointer: "the intent layer (ADR-0013/0015) compiles constructs onto this map; where this document says *regime*/*consumer*, the intent vocabulary spells the selector `dataplane` (ADR-0013:71)". Its §4 already carries ADR-0012's settled dpmcp count (`:181-183` "one per process … ADR-0012, DPMCP-I7 [verified]"), so the proposal's §4 housekeeping promise (proposal.md:134-135) is **met** — no further per-section edits.
- **PASS1-F16 — amend confirmed**, and it must land *before* archive (the sentence at `specs/intent-compiler/spec.md:47` is in the promotable delta): the subject is a tenant, whose field is `dataplane`.
- **PASS1-F17 (README:75) — amend**, one word ("the poll-mode dataplane's ADR-0012 draw"); it is the first-reader surface and `doc_examples.rs` pins only the TOML fences, not this prose.
- **Open question 2 (pre-change `openspec/specs/`) — confirmed and sequenced.** `openspec/specs/` today has *no* intent-compiler spec and a pre-change topology-config spec; the deltas are well-formed ADDED/MODIFIED blocks that merge at archive. The archive step is outstanding — run it, but **only after** PASS6-F1..F4 and PASS1-F16, or the retired label law and the stale `compile` signature get promoted verbatim.
- **Open question 3 (PASS1-F8, CHANGELOG) — re-dispositioned, not epic-local.** `cliff.toml` exists and the project instructions state CHANGELOG is "entirely managed with cliff automatically"; an empty `[Unreleased]` body before the first release tag is the pipeline's normal state, not a per-change omission — hand-amending it would fight the mechanism. The residual gap is standing: nothing runs `git cliff` yet (no released section exists at all). File one repo-wide bead: wire cliff generation into the release/tag flow, and reword ROADMAP DoD 5 to name the mechanism ("CHANGELOG flows from conventional commits **via cliff at release**"). Do not charge it to `intent-layer`; the identical clause sits in the archived `restool-baseline` tasks.
- **Open question 4 (baseline `regime` sweep) — stay in ADR-0012's vocabulary deliberately.** The nine `docs/baseline/dp*.md` files are board-verified records written in the sizing vocabulary; only `object-model.md` is cited as an INTENT invariant anchor (COVERAGE:195-199), so only it gets the F15 bridge note. The other ~40 hits are correct as-is once ADR-0012 itself carries the F9 pointer.

## Reviewed-clean (no rows)

- **README example vs shipped parser**: structurally pinned, not just re-checked — `crates/dpaa2-config/tests/doc_examples.rs` (task 3.3 F) feeds *every* ```` ```toml ```` fence of README.md **and** ADR-0013 through `parse_str`/`parse_schema`, so 3.3d's shape change could not have left a stale fence without a red test. README's fences are post-3.3d keyed tables; `[[crypto]]`/`[extra.router]` current.
- **topology-config spec delta**: matches the shipped parser per Pass 5's parse.rs truths — keyed tables, cross-family-only DuplicateName, reserved-kernel, name rule d13, `renamed` swap/chain semantics.
- **intent-compiler spec claims verified shipped**: `#[non_exhaustive]` on `Refusal` (`refuse.rs:47`) and `Dataplane` (`intent.rs:43`); `PoolShortfall` reservation documented (`refuse.rs:39`); dpseci `HAS_CG` (`compiled.rs:561`); dpsw `num_ifs`/`max_fdbs`/PER_FDB×2/`ctrl_if` (`compiled.rs:645-654`); the ADR-0005 amendment exists, cites ADR-0013, and resolves "ADR-0005 §N" through it (`docs/adr/0005-intent-layer-network-constructs.md:13-36`).
- **ADR-0002**: current — its ":42 three `compileLaws` predicates (INTENT_I7/I8/I9) marked for Apalache" agrees with the code and independently supports Pass 2's F8 resolution (align COVERAGE's marking on I7/I8/I9).
- **Proposal housekeeping delivered**: COVERAGE re-anchors DPDCEI-I1 to `intent/refuse.qnt TenantAbsent` (`COVERAGE.md:162`); object-model §4 dpmcp settled count present.
- **COVERAGE identity-laws table**: R15-linted both ways exactly as its preamble claims (`ledger.rs:1285-1303`).

---

## Fixed footer

**What was read (full):** all four prior pass reports; `openspec/changes/intent-layer/{specs/intent-compiler/spec.md, specs/topology-config/spec.md, specs/reconciler/spec.md, proposal.md}`; `docs/adr/{0005, 0014}` full, `0012:1-60`, `0013:500-529`, `0015:1-80`; `README.md`; `docs/ROADMAP.md`; `CHANGELOG.md`; `crates/dpaa2-config/tests/doc_examples.rs`. **Targeted:** `models/COVERAGE.md` (intent/raw/identity sections + DPDCEI-I1 row), `crates/dpaa2-api/src/refuse.rs:248-327` + `compiled.rs:247-263`, `ledger.rs` lint-rule greps, `docs/baseline/object-model.md` dpmcp grep, ADR-0002 intent grep, tree greps for `non_exhaustive`, `HAS_CG`/dpsw attrs, `match_board` in dpaa2-tools, `router/dpio/3`, `.beads/` (no jsonl export present — bead cross-checks deferred to synthesis with `bd`).

**Deliberately not read:** `design.md` beyond the label-rendering grep (Pass 1 owns its staleness map; D1/D5/D6a already dispositioned); `tasks.md` beyond the same grep; qnt/Rust bodies beyond the seam checks above (Passes 2/3/5 truths taken as given); the change's `specs/{provisioning-cli,formal-models}` deltas if any exist beyond the three found by glob — the specs/ dir grep covered all files present; ITF traces; ADR-0015 decisions 4–13 full text (Pass 1/3 citations taken as given).

**Open questions for synthesis:**
1. **Archive sequencing is the one hard ordering constraint this pass adds**: PASS6-F1..F4 + PASS1-F16 are edits to the promotable deltas and must precede `pnpm openspec archive intent-layer`; everything else can land after.
2. PASS6-F7's fix side (extend lint now vs bead): the R15 mechanism is a ~30-line clone; if synthesis batches an amend commit anyway, extending the lint in it is cheaper than a bead — but ADR-0014 accepts either.
3. Whether the PASS3-F14 bead exists yet (no `bd` access here, no jsonl export); PASS6-F8 rides on it either way.

**Token spend:** ~85k of the 120k budget (agent-reported); harness-measured ~130k total. All mandated scope covered; nothing left unreviewed.
