# Pass 2 — Isomorphism, twins, and code quality (`vocabulary-v2`, 34e708b..a578470)

Agent: software-architect. Budget ~130k; actual ~154k (rate-limit interruption mid-mandate-(d), resumed and completed; nothing partial).

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification |
|---|---|---|---|---|---|---|
| **PASS2-F1** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/intent/refuse.qnt:352-353` (used at `:367`) | copy-drift | — | misleads-a-reader | amend — `holderPool` returns the `""` sentinel D1 just outlawed, only to be `!= ""`-tested for PoolChain; the Rust twin (`crates/dpaa2-api/src/refuse.rs:869`) uses `matches!(.., Restricted {..})` and the model itself already has the clean idiom one file over (`derive.qnt:461` `match c.isolation { \| Restricted(_) => true \| _ => false }`). Replace `holderPool` with a bool-returning match; the hoisted-helper shape (the QNT translator workaround, refuse.qnt:162-164) is preserved | `grep -n 'holderPool\|!= ""' models/intent/refuse.qnt` → no sentinel test; model ladder green |
| **PASS2-F2** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/intent/types.qnt:94,103,111,132` (`from: str`, `"" = absent`) vs `crates/dpaa2-api/src/intent.rs:164,212,232,280` (`renamed: Option<_>`) | iso-violation | — | follow-up bead | The D6 law this change legislated (ADR-0002 amendment: "sentinel-for-case … violates the spec even when every trace matches") is violated by the very field rule 15 reads: the model tests `t.from != ""` (`refuse.qnt:399-417`) while Rust tests `renamed.is_none()` (`refuse.rs:949-994`). Pre-existing shape (ADR-0015 d10), but the change built a new rule pair on the divergent encodings and did not converge them or record a DEVIATION at `types.qnt:90-93` | after fix, `grep -n '"" = absent' models/intent/types.qnt` → empty (or a DEVIATION marker present); rule-15 predicates read arm-for-arm |
| **PASS2-F3** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/refuse.rs:480` (+ `openspec/changes/vocabulary-v2/tasks.md:33-34`) | doc-drift (twin-drift beneath) | — | breaks-a-claim | amend + follow-up bead — "no site can ever name `kernel` as absent" is false inside its own function: `Fabric.forwarded_by`, `Crypto.tenant`, `Extra.tenant` are `TenantName`, parse admits `kernel` there (`parse.rs:286` `resolves()` treats kernel as always-resolving) but compile rule 1 checks only declared names (`refuse.rs:503-526`; model identical `refuse.qnt:137-145`), so `TenantAbsent{Fabric/Crypto/Extra, "kernel"}` fires whenever neither `complete_kernel` (port-triggered, `main.rs:271-277`) nor `effective_tenants` (link-triggered, and invisible to the refusal, which reads `intent.tenants`) injected it. TOML-reachable: a hw fabric of tenant-only members forwarded by kernel, or `[[crypto]] tenant = "kernel"`, with no kernel port. Pre-existing behavior; the change's own claim text is what breaks. Scope the comment to the `TenantRef` sites; bead the three `TenantName` sites (D2 phase 2 or a decided non-goal) | unit test: `compile` on `Intent { fabrics: [hw fabric, forwarded_by "kernel", tenant members only] }` → today contains `TenantAbsent { referrer: Fabric(..), tenant: "kernel" }` |
| **PASS2-F4** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/intent.rs:66-72` vs `models/intent/types.qnt:43-44` | twin-drift | — | misleads-a-reader | amend — the named twins diverge on `""`: `tenantRefOf("")` → `Kernel` (the model-raw omitted encoding), `TenantRef::from_name("")` → `Named("")`. Vacuous through parse (omitted is `None`→`"kernel"` at `parse.rs:463`; explicit `tenant = ""` dies at `resolves`), but `from_name` is public API whose doc claims it is "the single normalisation… the KERNEL sentinel classification lives here, once" — it isn't; the `""` half lives in the model twin only. Either fold `""`→`Kernel` into `from_name` or note the deliberate domain split (Rust raw carries `Option`, so `""` is a name, not an omission) on both twins | pin the decided case with a doctest/unit: `TenantRef::from_name("".into())` |
| **PASS2-F5** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/refuse.rs:1738,1758,1780` | simplify | — | misleads-a-reader | amend — the three D4 parity tests assert `contains(..)` while the module doc (`refuse.rs:1123-1124`) states the convention is "asserting the *complete* set", which every neighbouring refusal test follows via `assert_eq!`; each parity fixture trips exactly its own singleton, so the stronger form is free and a future co-firing refusal cannot slip in unseen | switch to `assert_eq!(err(..), BTreeSet::from([..]))`; `cargo test -p dpaa2-api` green |
| **PASS2-F6** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/refuse.rs:282-299` | doc-drift | — | misleads-a-reader | amend — the F3-exemplar form names parse twin *and* raw pin, and `LinkSelfLoop` (`refuse.rs:272-277`) does exactly that (`RawLinkSelfLoop` + raw_itf); `RenameDoubleClaim` and `KernelDeclared` name only the parse twin, though their raw twins exist under *different spellings* (`RenamedFromDeclared` `intent_raw.qnt:161`, `ReservedKernel` `intent_raw.qnt:150`) — the one case where a reader most needs the pointer | `grep -n 'RenamedFromDeclared\|ReservedKernel' crates/dpaa2-api/src/refuse.rs` → one hit in each variant doc |

## Mandate verdicts

**(a) Structural isomorphism — clean except F1/F2.** `TenantRef` `Kernel | Named(str)` ⇔ `Kernel | Named(TenantName)`: sum-for-sum, no Default either side, names identical. `Isolation` `Public | Restricted(str) | Isolated` ⇔ `Public | Restricted{pool} | Isolated`: payload-for-payload (named field vs positional is spelling, which D6 excludes). `Referrer` six-for-six with the documented `Ref` prefix deviation, decoded correctly (`intent_itf.rs:246-257`, exactly the `MPort`⇒`Member::Port` precedent). Three parity refusals payload-identical (`LinkSelfLoop{link}`, `RenameDoubleClaim{construct,from}`, nullary `KernelDeclared` — the design's open question resolved the same way both sides). `REFUSALS_USE`/`REFERRERS_USE` QNT404 blocks complete at 25/6.

**(b) Parity-twin fidelity — faithful.** `link_self_loop_refusals` ⇔ `parse.rs:504` (same predicate on the parse-reachable domain; the pre/post-normalisation asymmetry is pass1 open-question 2, not re-litigated). `rename_double_claim_refusals` ⇔ `check_renames` arm-for-arm including the two-namespace split, self-rename inertness, and swap/chain admission; they differ only on cross-family duplicate names, which parse makes unrepresentable (footer Q1). `kernel_declared_refusals`'s exemption `*t != kernel_tenant(t.max_cores)` is exactly the design's "non-reserved shape" and no wider — any `max_cores` passes, but the kernel's `max_cores` is inert in derive (`derive.rs:461-467` reads `inv.cpus`), so the exemption's width has no derive-visible effect; model twin bit-identical. All six sites carry doc notes (F6 is the only gap in note *form*).

**(c) is_kernel survivors (51 hits, 8 files) — no missed D2 conversion.** Classification: **variant-arm wrappers on `TenantRef`** (the `matches!`-based `TenantRef::is_kernel`, `intent.rs:88` — a match in disguise, not a string test): `derive.rs:558`, `main.rs:273`, `parse.rs:768,908,1323,1372`, `compile_props.rs:174,183,268,270,857,878`. **Parse normalisation** (the D2-sanctioned boundary): `parse.rs:177,286`, `intent.rs:67`. **Legitimate `TenantName` comparisons** (fields D2 deliberately left as names): pool holder `refuse.rs:845`, `compiled.rs:508`; fabric forwarder `refuse.rs:686`, `compiled.rs:660`; root-kernel identity `derive.rs:498,551`, `compiled.rs:505`, `refuse.rs:936`, `main.rs:272`, `intent_pairing.rs:112`, `parse.rs:946`. **Local bools named `is_kernel` that test the *dataplane*, not a name**: `derive.rs:473,497,504-533,668-885`, `compile_props.rs:356+` (mildly confusable naming, cost-free). The one substantive residue of the names-not-refs sites is F3.

**(d) Deletion completeness — clean.** No dead arms, helpers, or fixtures: `effTenant` fully gone, `resolved()`/`tenantRefName` twin-used everywhere it should be, no orphaned tools snapshots, no reserved-token literals left in `dpaa2-tools`, decoder has no dead pool-pair arms (raw_itf's are the deliberate raw layer). The refuse.rs +350 is variants + rules + doc + tests with existing builders (`tenant`/`restricted`/`port`/`link`) reused; the only redundant addition is the test-module tombstone already filed as PASS1-F5 (not repeated). Rendering stayed Debug-derived (`render.rs:172-183`) — D3's "rendering derives from the enum" satisfied with zero new code, and `Port("pool")` vs `Pool("t")` are typographically distinct.

## Footer

**Read:** pass1 report; the change's `design.md`/`tasks.md`; `models/intent/{types,refuse}.qnt` in full; targeted regions of `derive.qnt`, `intent_raw.qnt`, `alphabet.qnt`, `main.qnt`; `crates/dpaa2-api/src/intent.rs` in full; `refuse.rs` (head 560, rules 12-15, compile, tests 1073-1250 and 1620-1900); `derive.rs:460-590` + resolved/tenantRefName call map; `crates/dpaa2-config/src/parse.rs:120-600`; `intent_itf.rs` decoder seams; `raw_itf.rs` raw-refusal head; `compiled.rs:490-530`; `matcher.rs` facet + 093.7 note; `dpaa2-tools` `main.rs:240-320`, `render.rs:150-234`, snapshots dir; repo-wide greps for `is_kernel`, `from_name`, `resolved()`, parity-refusal names, deleted-variant tokens.

**Deliberately not read:** `observed.qnt`/`vwire.qnt` bodies (pass1 owns their two findings); `derive.qnt`/`main.qnt` full bodies beyond the kernel-materialisation and directed-test regions (greps clean, effectiveTenants twin note verified verbatim against `derive.rs:568-573`); `compile_props.rs` bodies beyond the is_kernel hits; `render.rs` snapshot contents; anything requiring cargo/quint execution. Mandate (d) is complete; nothing partial.

**Open questions:**
1. `rename_double_claim_refusals` ORs all three construct families while parse's `construct_is_declared_unrenamed` early-returns on first family hit — they diverge only for cross-family duplicate names, unrepresentable from TOML but constructible programmatically (compile has no DuplicateName refusal; raw-only). Is duplicate-name a deliberate remaining one-sided D11 row, and if so where is that recorded?
2. `Isolation::Restricted { pool: TenantName::from("") }` is constructible programmatically on both sides (D1's "unrepresentable" holds only up to `TenantName` non-emptiness, which parse guarantees and the API does not) — the matcher test helper (`matcher.rs:869`) skirts this. Worth a line wherever F2's bead lands, since typestates are next.
3. F3's disposition needs a design owner: should compile treat `kernel` as always-resolving at the three `TenantName` reference sites (mirroring parse's `resolves`), or should parse stop admitting it there? Either answer closes the asymmetry; today each side gives a different one.
