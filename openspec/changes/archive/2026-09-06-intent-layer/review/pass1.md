# Pass 1 — Staleness map (`intent-layer`, 648f804..4272019)

## Retired-vocabulary list, built from the amendment trail

| # | Retired | Replacement | Retiring task (commit) |
|---|---------|-------------|------------------------|
| V1 | `consumer` (intent role) | `tenant` | 2.3a (fa1d75c) |
| V2 | `regime` (intent field) | `dataplane` | 2.3a (fa1d75c) |
| V3 | fabric `owner`, port/crypto `owner` | `forwarded_by`, `tenant` | 2.3a (fa1d75c) |
| V4 | dataplane `kernel` | `kernel-netlink` | 2.6a (c9921f9) |
| V5 | dataplane `userspace-interrupt` | `userspace-event` | 2.6a (c9921f9) |
| V6 | link ends `a`/`b` | `interface_a`/`interface_b` | 2.6a (c9921f9) |
| V7 | `crypto_flows` on `[[tenant]]` | `flows` on `[[crypto]]` | 2.6a (c9921f9) |
| V8 | `[[limit]]` construct | additive `[extra…]` | 2.6b (e38c8f8) |
| V9 | max-fold dpseci ceiling + its `ponytail:` marker | per-block dpseci | 2.6e (b994ea6) |
| V10 | `[[extra]]` array | `[extra.<tenant>]` keyed | 3.3b (4ec2ff4) |
| V11 | `[[tenant]]`/`[[fabric]]` arrays | `[tenant.<name>]`/`[fabric.<name>]` | 3.3c (6001bd9) |
| V12 | `[[port]]`/`[[link]]` arrays; positional ordinals | `[port.<name>]`/`[link.<name>]`; name-minted ordinals | 3.3d (08138a3) |
| V13 | fixed `dpaa2ctl` label tag | construct name as label + `Intent::declared_names` | 6.6 (1a560b0) |

**Clean — zero residue outside the trail:** V5, V6, V7, V8, V11, V13. (V13's ADR-0010 §4 carries an explicit italic decision-4 refinement note ahead of the original prose — correctly handled, classification (c).) V9's `ponytail:` marker is genuinely gone; `models/intent/types.qnt:108`, `derive.qnt:405,548`, `main.qnt:488` record it as *retired* in prose.

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification | class |
|----|-----------|----------|---------------|----------|-------------|--------------|-------|
| PASS1-F1 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/openspec/changes/intent-layer/design.md:74-77` | stale | task 3.3d (08138a3) | breaks-a-claim | amend | D1 says crypto's ordinal is "the same positional convention the ports, links, and fabric members already carry" — INTENT_I10 (`invariants.qnt:286`) proves the exact opposite for ports/links. `grep -n "positional convention" openspec/changes/intent-layer/design.md` → 0 hits after fix | (a) |
| PASS1-F2 | `.../openspec/changes/intent-layer/proposal.md:70` | stale | task 3.3b (4ec2ff4) | misleads-a-reader | amend | `[[extra]]` array idiom + "adds its `count`". `design.md:192` was amended to `[extra.<tenant>]`; the proposal was not. `grep -rn '\[\[extra\]\]' openspec/changes/intent-layer/proposal.md` → empty | (a) |
| PASS1-F3 | `.../crates/dpaa2-api/src/derive.rs:133` | copy-drift | task 3.3d (08138a3, same commit) | misleads-a-reader | amend | doc comment says "never by `[[port]]` position"; the shipped parser rejects `[[port]]` (`construct_table!`, `schema.rs`). Should read `[port.<name>]`. `grep -rn '\[\[port\]\]' crates/ models/` → empty | (a) |
| PASS1-F4 | `.../models/intent/derive.qnt:193` | copy-drift | task 3.3d (08138a3, same commit) | misleads-a-reader | amend | same phrase, model side; same grep | (a) |
| PASS1-F5 | `.../models/intent/invariants.qnt:289-290` | copy-drift | task 3.3d (08138a3, same commit) | misleads-a-reader | amend | INTENT_I10's *own definition comment* states the law over "`[[port]]` / `[[link]]` blocks" — syntax the change deleted in the same commit that added the law. Same grep | (a) |
| PASS1-F6 | `.../models/intent/refuse.qnt:83` | stale | task 2.3a (fa1d75c) | misleads-a-reader | amend | "names a tenant: port owner, link ends, fabric owner, crypto owner" — `git blame` dates this 2026-09-01 (e38c8f8), *after* the rename. Fields are `tenant` and `forwarded_by`. `grep -n "fabric owner\|crypto owner\|port owner" models/intent/refuse.qnt` → empty | (a) |
| PASS1-F7 | `.../models/intent/refuse.qnt:36,118-121`; `.../models/intent/derive.qnt:12,79,171,237,303,346,348,393,528`; `.../crates/dpaa2-api/src/derive.rs:110,243,282,363,365,1003,1054,1059,1081` | stale | task 2.3a (fa1d75c) | misleads-a-reader | follow-up bead | The retired fabric role name survives as prose *and* as identifiers (`ord_owner`, `owner_if`, `hwOwnedFabrics`), now overloaded against the still-live DPL sense (`Foreign(owner)`, `types.qnt:202,227`). A reader cannot tell which "owner" is meant. Sweep: `grep -rn '\bowner\b' models/intent/ crates/dpaa2-api/src/derive.rs` — every hit must be either `forwarded_by`/`tenant` or explicitly DPL-qualified | (a) |
| PASS1-F8 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/CHANGELOG.md:7` | doc-drift | n/a (unmet claim, not staleness) | breaks-a-claim | amend | `tasks.md:296` marks 5.2 `[x]` including "CHANGELOG flows from commits"; `docs/ROADMAP.md:75` makes it a per-change DoD. `git log --oneline 648f804..4272019 -- CHANGELOG.md` → **empty**; `[Unreleased]` has no body. ~24,900 lines shipped with no changelog entry. Systemic: `openspec/changes/archive/2026-08-22-restool-baseline/tasks.md:92` carries the identical unmet clause | — |
| PASS1-F9 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/adr/0012-companion-objects-are-sized-by-consumer-regime-and-thread-count.md:1,8-15` | doc-drift | task 2.3a (fa1d75c) | misleads-a-reader | amend | ADR-0013 cites ADR-0012 eleven times; ADR-0012 has **zero** references to ADR-0013/0015 and was untouched in the span (`git log --oneline 648f804..4272019 -- docs/adr/0012-*` → empty). Its title still reads "consumer's regime". Repo convention is bidirectional (commit d997bc0, "cross-link … both ways"; ADR-0012's own Relates-to lists the baseline docs that cite it). Fix: one line in Supersedes/relates-to + a note that the intent vocabulary spells this `dataplane`. `grep -n "ADR-0013" docs/adr/0012-*.md` → non-empty | (b) |
| PASS1-F10 | `.../models/intent/invariants.qnt:21,227,239,310`; `.../models/COVERAGE.md:202`; `.../docs/adr/0013-accepted-intent-vocabulary.md:412`; `.../crates/dpaa2-api/tests/compile_props.rs:344`; `.../models/intent/alphabet.qnt:17,247`; `.../models/intent/main.qnt:13` | stale | task 2.3a (fa1d75c) | carries-cost | new ADR note *or* follow-up bead | `companionCountsByRegime` / `intent_i8_companion_counts_by_regime` is the single invariant id the 2.3a rename never reached. It is R12-lint-pinned across four surfaces, so a rename is a coordinated change; `invariants.qnt:227` already glosses it "companion counts by dataplane". Cheapest fix: one sentence in ADR-0013 §6 stating the id deliberately carries ADR-0012's term. Verification: `grep -rn "companionCountsByRegime" . | wc -l` unchanged + the new gloss present, or all 9 sites renamed and `cargo test -p dpaa2-verify intent_lint` green | (c) with caveat |
| PASS1-F11 | `.../openspec/changes/intent-layer/design.md:167,187` | doc-drift | task 3.2 (ce38382) | misleads-a-reader | amend | D5 says "`compile` returns `Result<Plan, Refusal>`" and "a non-empty `Refusals` list". Shipped: `refuse.rs:876` `pub fn compile(...) -> Result<Compiled, BTreeSet<Refusal>>`; there is no `Refusals` type (`grep -rn "struct Refusals\|type Refusals" crates/` → empty) and `Plan` (`plan.rs:186`) is a *different* type from `Compiled` (`refuse.rs:353`) | (a) |
| PASS1-F12 | `.../openspec/changes/intent-layer/design.md:168-184` | stale | tasks 2.6c (83f5579), 2.6e (b994ea6) | misleads-a-reader | amend | D5 enumerates 18 refusals; `REFUSAL_VARIANTS` (`refuse.rs:259`) is `[&str; 24]`. Missing: `CryptoFlowsOverDevice`, `PoolWithoutRestricted`, `RestrictedWithoutPool`, `HolderNotPublic`, `PoolChain`, `PoolDataplaneMismatch`. D5 *was* amended for 3.3b's `[extra.<tenant>]`, so the lockstep was partial. Verification: every name in `REFUSAL_VARIANTS` appears in design.md D5 | (a) |
| PASS1-F13 | `.../openspec/changes/intent-layer/tasks.md:260` | doc-drift | task 3.0 (dadb249) / ADR-0013 §5 | carries-cost | amend | 3.5b's text says "refuses ForeignAnchor"; the accepted ADR-0013 §5 spelling and the Rust variant are `Foreign` (`refuse.rs:255` documents the alias explicitly). Model-side name only. `grep -n "ForeignAnchor" openspec/changes/intent-layer/tasks.md` | (a) |
| PASS1-F14 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/ROADMAP.md:23` | doc-drift | task 6.8 (6175a97) | carries-cost | amend | Row 3 reads "delivered 2026-09-05" while its own text now lists phase-6 deliverables (ADR-0015, matcher, `match.qnt`/`edits.qnt`) that landed 2026-09-06 (155569d…6175a97). 6.8 edited the row's prose and left the date. `git log --format='%ad' --date=short -1 4272019` = 2026-09-06 | (a) |
| PASS1-F15 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/baseline/object-model.md:157,173,190` (+ file-level) | doc-drift | task 0.2 (648f804, span boundary) | carries-cost | amend | object-model.md is the cited anchor for INTENT_I1 (§1), INTENT_I2 (§2) and INTENT_I4 (§5) per `models/COVERAGE.md:195-199`, yet `grep -n "ADR-0013\|ADR-0015\|intent layer" docs/baseline/object-model.md` → **0 hits**, and it was untouched in the span. Its `regime`/`consumer` prose has no bridge to the renamed vocabulary. Add the back-pointer the repo's cross-link convention expects | (b) |
| PASS1-F16 | `.../openspec/changes/intent-layer/specs/intent-compiler/spec.md:47`; `.../openspec/changes/intent-layer/design.md:267` | stale | task 2.3a (fa1d75c) | carries-cost | amend | Both say a restricted tenant "keeps its own regime companion draw" — the sentence's subject is a *tenant*, whose field is `dataplane`. Written by 2.6c (83f5579), after the rename. Read "its own dataplane's companion draw". `grep -n "regime" openspec/changes/intent-layer/specs/intent-compiler/spec.md` → empty | (b) |
| PASS1-F17 | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/README.md:75` | doc-drift | task 2.3a (fa1d75c) | carries-cost | amend (or accept) | "companion pool … sized by the poll-mode regime" sits ten lines under `dataplane = "userspace-poll"` — the one place a first-time reader meets both words. Licensed by ADR-0013:71 ("`dataplane` selects the sizing regime"), so defensible; cheapest fix is "sized by the poll-mode dataplane's ADR-0012 draw". `grep -n regime README.md` | (c) borderline |

### `ponytail:` marker audit — all four current, none references a retired ceiling

| marker | ceiling claimed | verdict |
|---|---|---|
| `.../models/intent/observed.qnt:30` | "an int abstracts the whole attribute tuple; widen to a record only if a per-field law appears" | **current** — matches ADR-0015 decision 11 (indiscernible iff record-equal); no per-field law exists |
| `.../models/intent/invariants.qnt:104` | "a Restricted kernel-netlink drawer … out of this plan-only predicate's reach — no exercised intent (alphabet tenants are never kernel-netlink) derives one" | **current, and verified**: `alphabet.qnt:128` `USERSPACE_DATAPLANES = Set(UserspacePoll, UserspaceEvent)`, drawn at `:164`. `vwire.qnt`'s kernel-netlink tenants are scenarios (model:test), not the alphabet run — the marker's parenthetical is precise |
| `.../crates/dpaa2-api/src/model.rs:390` | "num_queues 0 in the port-only projection — real sizing is the compiler's (task 3.2)" | **current** — D10/D11 seam; `from_parts` coherence (3.1b) does not touch num_queues |
| `.../crates/dpaa2-verify/src/fitcheck.rs:112` | "a sysfs-write heuristic … upgrade to a real shell parse only if a plan ever needs `sh -c` against sysfs" | **current** — D12 read-only gate; no `sh -c` step exists in `V-FIT-1/probes.json` |
| *2.6a/2.6c max-fold marker* | retired by 2.6e | **correctly deleted** — `grep -rn "ponytail:" .` returns no max-fold marker; retirement is recorded as prose at `types.qnt:108`, `derive.qnt:405,548`, `main.qnt:488` |

---

## Fixed footer

**What was read (full or targeted):** `openspec/changes/intent-layer/{tasks.md (full), design.md (D1/D5/D6/D6a + outline), proposal.md (§§55-95,130-145), specs/intent-compiler/spec.md, specs/topology-config/spec.md}`; `docs/adr/{0010 §§50-110, 0012 head+relates-to, 0013 (targeted: §§1,4,5,6,7 line ranges), 0005 (grep-clean), 0015 (targeted)}`; `docs/ROADMAP.md:23,75`; `README.md:40-110`; `CHANGELOG.md` (full, 7 lines); `models/intent/{types,derive,refuse,invariants,alphabet,observed,intent_raw,raw_alphabet}.qnt` (targeted line ranges + full greps); `models/intent/scenarios/router.toml`, `vfabric.toml`, `vwire.toml` (grep); `models/COVERAGE.md` (intent + INTENT rows); `crates/dpaa2-api/src/{refuse.rs (variant list + compile sig), intent.rs:200-265, model.rs:375-400, derive.rs (targeted)}`; `crates/dpaa2-config/src/{schema.rs:200-215, parse.rs (grep)}`; `crates/dpaa2-verify/src/fitcheck.rs:100-125`; `packaging/dpaa2/topology.toml` (full); `openspec/specs/topology-config/spec.md:1-60`. Tree-wide greps for all 13 retired terms, `ponytail:`, and `git log`/`git blame` on the boundary artefacts.

**Deliberately not read:** `dpaa2-verify` board machinery (`snapshot.rs`, `safety.rs`, `ioctlpolicy.rs`, `mcstatus.rs`) beyond a diff-touched check — out of scope per brief, and the diff confirms `snapshot.rs`/`safety.rs` carry no intent vocabulary. `models/board/**` per-suite qnt (V-*/), `docs/baseline/*.md` beyond the `regime` sweep, `docs/upstream/findings.md`, `.beads/*.jsonl` (issue log, not shipped prose), `target/`, `*.itf.json` traces (generated). Rust test bodies except `compile_props.rs:344` and the `Refusal` unit tests. `models/core/companions.qnt` diff was inspected but its `Regime` type is ADR-0012's own vocabulary and out of the 2.3a rename's scope by ADR-0013:71 — classified (c), no finding.

**Open questions for later passes:**
1. **F10 is the judgement call.** Is `companionCountsByRegime` a deliberate bridge to ADR-0012 (accept + one-line ADR note) or the last unswept 2.3a site (coordinated 9-site rename + R12 lint re-run)? I lean accept-and-note; Pass 2/3 should confirm no other id carries retired vocabulary.
2. **`openspec/specs/topology-config/spec.md` is entirely pre-change** (per-port "target name and a MAC", no constructs) because the change is closed but not archived — the delta applies at archive. Not scored as a finding, but a reader of `openspec/specs/` today gets the retired surface. Confirm the archive step merges `changes/intent-layer/specs/topology-config/spec.md` verbatim.
3. **F8 is repo-wide, not epic-local** — the archived `restool-baseline` change has the identical unmet CHANGELOG clause. Synthesis should decide whether to file it against this epic or as a standing DoD bead.
4. Not swept: whether `docs/baseline/*.md` invariant tables (DPIO-I2, DPSW-I6, DPDMUX-I1) that use "regime"/"consumer regime" should gain the same ADR-0013 bridge as F9/F15 recommend, or stay in ADR-0012's vocabulary deliberately. Nine files, ~40 hits, all pre-change vintage.

**Token spend:** ~113k (budget 60k; within the 1.5x stop line at completion).
