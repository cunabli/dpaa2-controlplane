# Pass 1 — Residue, deferrals, verify re-run (dpni-typestate)

Span re-verified: `ced8de8^..5508c75`, 26 commits, 95 non-openspec/non-board files. All commands read-only; no cargo build/test run.

## Findings ledger

| ID | file:line (absolute) | category | superseded-by | severity | disposition | verification |
|---|---|---|---|---|---|---|
| **PASS1-F1** | `repo:docs/baseline/dpni.md:149-171` (Attribute mutability), `:484-489` (register #1), `:513-521` (#4), `:541-544` (#7), `:554-557` (#11) | doc-drift | task 6.1 / `dc64596` (the close-out whose checked box asserts "deferral rows verified … pointing at their tiles") | breaks-a-claim | amend — add the tile pointer to each row | `grep -cE 'mc-portal-backend|\(#10\)|\(#9\)' docs/baseline/dpni.md` ≥ 4 (today: **0**) |
| | The brief's "thin grep surface" is confirmed and worse than 2: the baseline carries **exactly one** genuine tile pointer, `:562` (`num_rx_tcs` → `dpl-tape-out`, #14). The other grounding hit, `:540` "(register #10, V-DPNI-5)", is the *unknown-register* numbering, a false positive. Missing entirely: #1 TX_CONF v1 handler → #10; the 35 non-primary-MAC `dpni_set_*` setters → #10 (`:167-171` says only "the rest have no consumer in this corpus"); register #4's QoS/FS half → #9/#10 (`:521` "still unread"); #7/#11 → #9/#10. Design D7 (`design.md:110-115`) names all four explicitly. | | | | | |
| **PASS1-F2** | `repo:models/COVERAGE.md:84-96` (DPNI block) | doc-drift | task 6.1 / `dc64596` | misleads-a-reader | amend — COVERAGE is the per-tile deferral ledger for every other family; land the missing rows there, not only in prose | `grep -n 'num_rx_tcs.*#14\|runtime setter.*#10' models/COVERAGE.md` non-empty |
| | Complementary half of F1. COVERAGE **has** TX_CONF → #10 (`:95`, DPNI-I11/V-DPNI-4). It has **no** row for the 35 runtime setters → #10, **no** row for `num_rx_tcs`-via-DPL → #14 (that one lives only in the baseline), **no** row for the table/traffic items. Net: neither document carries the full promised set; each carries a disjoint slice. | | | | | |
| **PASS1-F3** | `repo:crates/dpaa2-verify/src/intent/dpni_itf.rs:97-101` vs `repo:models/families/dpni.qnt:83-88,260,474-475` | twin-drift | task 5.2 — the `McClearedFlags` law (`77be907`) made `HasReplication` load-bearing in the model's escape vocabulary; the parser touched at `58f0ccd` was not extended | misleads-a-reader | amend — one match arm | `grep -c 'HasReplication' crates/dpaa2-verify/src/intent/dpni_itf.rs` == 1 (today: **0**) |
| | `raw_escape()` decodes only `"PfdrInPeb"` and errors `unknown RawEscape tag` on anything else. The model's `RawEscape` has two constructors and `HasReplication` appears inside `mcCleared` (`dpni.qnt:260`) and the `McClearedFlags` witness set (`:474-475`). The Rust core *can* represent it (`families/dpni.rs:260,283,355`). So a regenerated trace touching the cleared-escape face dies at decode, and `dpni_replay.rs:17-19` would mis-attribute that to "a model↔core divergence made loud" when it is a parser gap. | | | | | |
| **PASS1-F4** | `repo:crates/dpaa2-verify/src/intent/dpni_itf.rs:54,183` + `repo:crates/dpaa2-verify/tests/dpni_replay.rs:145-152` | dead | — (coverage gap, no staleness claimed) | carries-cost | follow-up bead: freeze `scenarioUnpricedRefusedTest` into `dpni_scenario` + `package.json:15` match list, or delete the arm | `grep -l Unpriced models/traces/families/dpni/*.itf.json` returns a file (today: **none**) |
| | `DpniRefusal::UnpricedDataplane` — decoder arm and replay witness are dead against the committed corpus. The model *has* the run (`dpni_lifecycle.unpricedDataplaneRefusedTest`, `dpni.qnt:542`) and the scenario module *has* the arm (`dpni.qnt:670-671`), but no `dpni_scenario` run drives it, so it is never frozen. Exact precedent already on the record: intent-layer synthesis, "the warning decoder tags in `intent_itf.rs` are dead code against the committed corpus". | | | | | |
| **PASS1-F5** | `repo:models/README.md:180-181` (also `:213`, `:259`) | doc-drift | `5508c75` | misleads-a-reader | amend — the tree node and two prose lines | `grep -n 'families/dprc\|families/dpni' models/README.md` non-empty (today: **empty**) |
| | Tree still reads `traces/ committed frozen ITF traces of the retro runs (board suite traces live beside their module)`. Post-move that root holds `families/dprc` (19), `families/dpni` (7), `retro` (2). `models/README.md` **was** touched by the epic (`:137-142`, dpni.qnt node) so the file was open and the tree node was missed. | | | | | |
| **PASS1-F6** | `repo:CHANGELOG.md:7` | doc-drift | — (unmet obligation, not staleness) | misleads-a-reader | amend the task wording ("cliff at release") **or** run cliff | `git log --oneline -1 -- CHANGELOG.md` names a dpni commit, or `tasks.md:60` drops the clause |
| | Task 6.1 is checked with "CHANGELOG via cliff"; `CHANGELOG.md` has not been touched since `090b1b8` (scaffolding) and `## [Unreleased]` is empty. `cliff.toml` exists. **Not a regression**: dprc-encapsulation task 6.1 carries the identical clause and also left the file untouched — it is a repo-wide convention drift, so the fix is one wording decision, not per-epic work. | | | | | |
| **PASS1-F7** | `repo:openspec/changes/dpni-typestate/tasks.md:57-58` | doc-drift | design D7 as written at `ced8de8` (`design.md:114`) | misleads-a-reader | amend — "TX_CONF v2" → "TX_CONF v1 handler" | `grep -n 'TX_CONF v1' openspec/changes/dpni-typestate/tasks.md` |
| | Tasks says the deferral is "TX_CONF v2"; D7 and baseline register #1 both say the open question is whether MC 10.39 retains a **v1 handler** for a 10.32-built client. Inverted. | | | | | |
| **PASS1-F8** | `repo:crates/dpaa2-verify/README.md:11-14` | doc-drift | task 5.1 (`641b4d5`) | carries-cost | amend — one line, or fold into a family-replay section | `grep -n 'dpni_replay' crates/dpaa2-verify/README.md` non-empty |
| | "What it does today" names only the retro reconciler replay. The epic added a fourth replay rung (`tests/dpni_replay.rs`); `tests/dprc_replay.rs` was already unlisted, so this is the second miss on the same line. The file **was** edited by `5508c75` (path prose), so it was open. | | | | | |
| **PASS1-F9** | `core/model.rs:478`, `tests/compile_props.rs:168`, `models/intent/invariants.qnt:105` | — (report-only) | n/a | n/a | none — prior changes' ledger | `git blame` shows `620feec`/`549928c` (2026-09-06, bead 9yy) and `83f5579` (2026-09-01, intent task 2.6c), all ancestors of `ced8de8` |
| | `ponytail:` sweep found **3 markers in epic-touched files**, but all three pre-date `ced8de8`. The grounding fact "zero `ponytail:` markers in epic-touched code" is imprecise: the *files* are touched, the *markers* are not this epic's. No epic commit introduced one. Reported so Passes 2/3 do not re-raise them as this change's residue. | | | | | |

No duplication findings this pass (the one candidate is protected — see below).

## Classification summary (for Passes 2–4)

| Hit | Class | Note |
|---|---|---|
| F1 baseline tile pointers absent | **(a) stale** | task 6.1 should have written them |
| F2 COVERAGE deferral rows absent | **(a) stale** | task 6.1 should have written them |
| F3 `raw_escape` missing `HasReplication` | **(a) stale** | task 5.2 grew the model vocabulary past the parser |
| F4 `UnpricedDataplane` dead arm | **(a) stale** (coverage) | task 5.1's freeze set should have carried it |
| F5 `models/README.md` traces tree | **(a) stale** | `5508c75` |
| F6 CHANGELOG untouched | **(a) stale** (repo-wide, pre-existing) | task 6.1 wording |
| F7 "TX_CONF v2" | **(a) stale** | D7 says v1 |
| F8 verify README rung list | **(a) stale** | task 5.1 |
| F9 three `ponytail:` markers | **(c) historical** | pre-epic, out of span |
| `dpni_scenario` restates 3 create actions | **(b) deliberate** | see reviewed-clean |
| baseline `:540` "register #10" | **(c) historical** | unknown-register numbering, not a tile |

## Reviewed-clean (do not re-litigate)

- **Scaffolding sweep: zero hits** across all 95 touched files for `todo!`, `unimplemented!`, `dbg!`, `#[allow(dead_code)]`, `#[ignore]`, `TODO`, `FIXME`, `XXX`, `HACK`, `WIP`. No commented-out code in any touched `.rs` (the one regex near-hit, `crates/dpaa2-verify/tests/dprc_replay.rs:298`, is a prose sentence continuation).
- **Trace corpus: 7/7 replayed, zero orphaned, guard mechanised.** `models/families/dpni.qnt:705-767` defines exactly 7 `dpni_scenario` runs; `package.json:15` `model:freeze-dpni --match` names the same 7; `dpni_replay.rs:35-63` `TRACES` lists the same 7; `dpni_replay.rs:287-315` `every_committed_trace_is_listed` asserts **both** directions (listed⇒on-disk and `on_disk.len() == TRACES.len()`), so a dropped or unlisted file fails CI.
- **Trace-corpus move (`5508c75`) left no dangling paths.** `grep -rn "models/families/traces"` → zero repo-wide; no surviving bare `models/traces/<name>.itf.json` reference. `itf.rs:1`, `dpni_itf.rs:1-2`, `dprc_itf.rs:1-3`, `lastverbs_trace.rs:27`, `retro_replay.rs:31`, `dpni_replay.rs:68,295`, `dprc_replay.rs:117,668` and `package.json:10-15` all carry the new paths. Only F5's prose lagged.
- **guu.4a stale-wording sweep clean.** No "unbounded"/"no upper bound"/"any int" num_queues claim survives in `dpni.qnt`, `intent/{derive,refuse,invariants,main}.qnt`, `families/dpni.rs`, `intent/derive.rs`, `docs/baseline/dpni.md`, ADR-0011/0013 or the openspec artifacts. The fence is textually two-sided: `dpni.qnt:229` `inRange(cfg.numQueues, 32)`, `refuse.qnt:73,286,291`, `families/dpni.rs` `NumQueues::HI`, `refuse/tenant.rs:108,120`, `refuse/mod.rs:201,344,381`, `intent_itf.rs:770`, ADR-0013:384, baseline:380-384, ROADMAP:25. *Whether the two predicates are the same predicate is Pass 2's mandate (b), not mine.* `derive.rs:105` carries the corrected comment ("Off-envelope is now fenced by QueueEnvelopeExceeded (refuse.qnt rule 7; bead guu.4a)").
- **Task 3.2 hazard closure holds offline.** No `Default` derive or impl on `Isolation`/`Intent`/`TenantName` (`tenant.rs:32,104,134,159` derives all omit it; `impl Default for …` → none). `TenantName` is deliberately excluded from the `resource_name!{@sentinel …}` arm that mints `empty()` (`core/types.rs:25-27,44`), and **no surviving `TenantName::empty()` call site** exists anywhere in `crates/` — the `6fdd3c8`/`11e961b` ripple is complete.
- **Task 1.1 Apalache mark is real.** `package.json:8` `model:verify` runs `quint verify models/families/dpni.qnt --main=dpni_lifecycle --invariant=stateInvariants --apalache-version=0.56.1`; the conjunction at `dpni.qnt:504` carries all six named invariants; `package.json:3/4/5` include dpni.qnt in typecheck/test/simulate.
- **Task 4.2 / D5-z5z rider satisfied.** `contract/mc.rs:118-120` cites V-DPRC-13-rev1, flat 203, "recorded as a spawn-count/latency fix, not a leak fix"; `COVERAGE.md:84` carries the same OI-3 disposition. `engine.rs:227` is the per-candidate `observe_container(id)` call.
- **Protected deliberate duplication — do not fold.** `models/families/dpni.qnt:625-690` (`dpni_scenario`) restates the three create actions that `dpni_lifecycle:340-370` already has. It *imports* every semantic (`inEnvelope`, `deriveProfile`, `profileCfg`, all refusal constructors, `dpni.qnt:631-644`) and restates only the `var world` plumbing — Quint offers no way to lift a var-mutating action across modules. Documented at `dpni.qnt:615-624`. Fold mechanism: none available; readability cost of any workaround exceeds the 20-line restatement.
- **Option vocabulary counts match:** 10 `DpniOpt` constructors in `dpni.qnt:56-66` == 10 in `families/dpni.rs:165-200` == 10 decoder arms in `dpni_itf.rs:82-92`.

## Open questions for later passes

1. **Pass 2 (mandate b):** `scenarioEnvelopeRefusedTest` replays `DpniRefusal::RangeViolation` (`dpni_replay.rs:45-48`, witness `:118-123`), **not** `Refusal::QueueEnvelopeExceeded`. The intent-side fence is unit-tested (`refuse/compile_tests.rs:464,481`) and simulate-witnessed (`package.json:6` `wQueueEnvelopeExceeded`), but `grep -l QueueEnvelopeExceeded models/intent/traces/*.itf.json` → **no file**: no frozen ITF trace anywhere carries it. Is the guu.4a fence's *intent* arm ITF-replayed, or only unit-tested? The brief's mandate says the trace must replay the refusal arm.
2. **Pass 2 (mandate c):** decoder-arm coverage vs corpus — 5 of 10 `DpniOpt` arms (`TxFrmRelease`, `HasPolicing`, `SharedCongestion`, `NoMacFilter`, `StashingDis`) and 1 of 2 `RawEscape` arms appear in no frozen dpni trace (tag histogram over the 7 files: only `SingleSender`, `CustomCg`, `HasKeyMasking`, `HasOpr`, `OprPerTc`, `PfdrInPeb`). They are covered by `families/dpni.rs` unit tests. Intended split or a freeze-set gap?
3. **Pass 4:** `models/board/V-DPNI-4` does not exist (V-DPNI-1,2,3,5,6,7,8,9,10 do). `COVERAGE.md:95` cites "V-DPNI-4" as the raw-command probe deferred to #10 — a suite id reserved for a probe that cannot run. Deliberate reservation or a dangling cite?
4. **Pass 4:** F6's CHANGELOG clause is inherited verbatim from dprc-encapsulation task 6.1. Fix the wording once in the DoD template rather than per-epic?

## Footer

**Read in full:** `openspec/changes/dpni-typestate/{brief.md (review/), tasks.md}`; `crates/dpaa2-verify/tests/dpni_replay.rs` (336 lines); `models/COVERAGE.md` DPNI blocks (`:84-96`, `:355-400`); `docs/baseline/dpni.md` §Attribute mutability, §Intent mapping, §Invariant candidates, §Unknown register (`:145-200`, `:370-400`, `:467-562`); `models/families/dpni.qnt:53-110, 200-230, 418-510, 615-780`; `crates/dpaa2-verify/src/intent/dpni_itf.rs:45-200`; `crates/dpaa2-api/src/core/types.rs:20-60`; `crates/dpaa2-api/src/intent/tenant.rs` (grep surface); `package.json` script block; `models/README.md:130-260`; `crates/dpaa2-verify/README.md` (grep + `5508c75` diff); `docs/ROADMAP.md` tile table; `CHANGELOG.md`; `design.md` Goals/Non-Goals + D7/D8; ADR-0011/0013 epic diffs.

**Deliberately not read:** `models/board/**` (operator-sealed, out of scope); ADR-0019 content (Pass 4's); the substance of `restool.rs`/`parse.rs`/`engine.rs`/`tenant.rs` sans-io behaviour (Pass 3's); `families/dpni.rs` typestate semantics beyond the grep surface (Pass 2's); the six spec deltas under `openspec/changes/dpni-typestate/specs/` (Pass 4's); archived review records touched only by `5508c75`'s mechanical path churn (explicit out-of-scope).

**Not runnable here (attested green per 6.1 DoD, not re-run):** `cargo build|fmt|clippy|doc|test`, `pnpm model:*`. Every verify obligation above was re-checked by grep/read against the cited names and assertions.
