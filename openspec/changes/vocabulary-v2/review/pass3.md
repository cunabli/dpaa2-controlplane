# Pass 3 — Spec/docs alignment (`vocabulary-v2`, 34e708b..a578470)

Agent: spec-align. Budget ~90k; actual ~93k.

## Findings

| ID | file:line | category | superseded-by | severity | disposition | verification |
|---|---|---|---|---|---|---|
| **PASS3-F1** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/openspec/changes/vocabulary-v2/design.md:238-242` (Migration Plan) vs the commit series and `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/CHANGELOG.md:7` | doc-drift (unfulfilled promise) | — none; the promise was never executed | breaks-a-claim | amend: the Migration Plan promised the semver-major changelog note "via the conventional-commit `!`". No execution commit carries it — subjects verified from the reflog: 42dfe06 `model+api+config+verify+tools: the pool lives inside Restricted…`, a50f412 `…one TenantRef sum owns…`, 722d5c0, 34c332f, 8e29281, b25e79a, a578470 — none has `!` or (visible) a `BREAKING CHANGE` footer, and `CHANGELOG.md` is an empty cliff-managed `[Unreleased]`, so the major note can never materialize. Commits are amendable-until-sealed per repo convention: reword the two BREAKING commits (1.1, 2.1) to `…!:`; otherwise amend design.md to record the deviation | `git log --format='%s%n%b' 34e708b..a578470 \| grep -E '^[a-z+]+(\([^)]*\))?!:\|BREAKING CHANGE'` non-empty; cliff output shows a major section |
| **PASS3-F2** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/crates/dpaa2-api/src/matcher.rs:121` (also `:865`; echoed in `openspec/changes/vocabulary-v2/tasks.md:77-78`) | twin-drift / doc-drift | born wrong in b25e79a (task 5.2, bead 093.7) | misleads-a-reader | amend: the 093.7 note claims "the model twin is `match.qnt`'s `ConfigFacet` `Tenant.pool`" — `match.qnt` contains no `ConfigFacet` and no `pool` at all. The model-side note actually landed in `models/intent/observed.qnt:32-37`, and it states the *opposite* relationship ("this abstract int has no `pool` … no model twin to drift against"). Repoint matcher.rs:121/:865 to observed.qnt's honesty block; tasks.md 5.2's "noted at matcher.rs and `match.qnt`" carries the same wrong location | `grep -rn 'ConfigFacet' models/intent/*.qnt` → observed.qnt comment only; `grep -n "match.qnt" crates/dpaa2-api/src/matcher.rs` no longer claims a `ConfigFacet` pool twin |
| **PASS3-F3** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/models/COVERAGE.md:289-296` vs `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/package.json:6` | doc-drift | 8e29281 (task 5.1 — the section claims "re-run for the vocabulary-v2 raw-layer changes" while omitting the run's own witnesses) | carries-cost | amend: the wired raw coverage run requests 18 witnesses; COVERAGE.md records 13. Missing: `wKernelDeclared` (raw structure witness, `raw_alphabet.qnt:186`) and `wRawPortPresent`/`wRawLinkPresent`/`wRawFabricPresent`/`wRawExtraPresent` (`raw_alphabet.qnt:187-190`). The intent-side section records its structure dimensions (COVERAGE.md:240-243); the raw side records none, under the same "single authoritative copy of the counts" banner (line 214). Add a raw structure-dimensions line, or state they are deliberately unrecorded | every witness token in the raw `model:coverage` invocation appears in COVERAGE.md: `grep -o 'w[A-Za-z]*' package.json` (raw segment) each greps non-empty in `models/COVERAGE.md` |
| **PASS3-F4** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/docs/adr/0002-formal-methods-process.md:81-83` vs bead 093.7's shipped decision | doc-drift (law vs recorded exception) | — | misleads-a-reader | amend (one sentence): the amended law says "a flattened field for a variant payload — violates the spec even when no trace ever distinguishes them"; the same change's 093.7 decision keeps exactly that shape (`ConfigFacet::Tenant.pool` flat, empty-`TenantName` "no pool" at `matcher.rs:114,123`) as deliberate. The reconciliation — projection types whose model twin is an abstract `int` sit outside the law — exists only in code comments (`observed.qnt:32-37`). Not re-litigating 093.7; the ADR stating the law should carry its one standing exemption so the next reader doesn't flag the matcher as a violation | `grep -n 'flattened' docs/adr/0002-formal-methods-process.md` block names the projection/abstract-facet exemption or cites the observed.qnt honesty-block mechanism |
| **PASS3-F5** | `/workspace/dev/clearfog-cx-lx2/src/dpaa2-controlplane/openspec/changes/vocabulary-v2/proposal.md:90` | copy-drift | — (wrong at authoring, 34e708b) | carries-cost (minor) | amend: "regenerated witnesses under `models/traces/`" — the intent corpus lives at `models/intent/traces/`; `models/traces/` is the retro corpus | `ls models/intent/traces/` holds the regenerated ITF; proposal names the right path |

## Pass 1 doc-hit dispositions (mandated)

| ID | verdict |
|---|---|
| **PASS1-F1** (`models/intent/scenarios/vwire.qnt:32-33`) | **Confirmed stale, endorse amend.** The comment narrates "pool set keeps RestrictedWithoutPool/PoolWithoutRestricted quiet" — both variants deleted by 42dfe06 (task 1.1); the mechanism it describes (refusals that exist but stay silent) no longer exists. Reword to: the shapes are unrepresentable since D1 (`Isolation::Restricted { pool }`), the twin exercises only the surviving `PoolDataplaneMismatch`. Verification per Pass 1's grep. |
| **PASS1-F2** (`docs/adr/0013-accepted-intent-vocabulary.md:462`) | **Confirmed stale, endorse amend-by-name.** The pin `types.qnt:35-37` now lands on the `TenantRef` comment/type (inserted by a50f412, task 2.1); the regime-bridge sentence it means lives in the `Dataplane` comment, now `types.qnt:60-62`. Repoint by name ("the `Dataplane` comment in `types.qnt`"), not by line — the repo's other cross-refs name functions, and this pin has now gone stale once already. |

## Alignments verified (no action)

- `[models/intent/refuse.qnt:84 + crates/dpaa2-api/src/refuse.rs:299 + crates/dpaa2-verify/src/intent_itf.rs:748,756 + models/intent/alphabet.qnt:321 + docs/adr/0013:410-417 + specs delta]` — **Alignment** — Design's Open Question resolved to the *empty* `KernelDeclared` payload, consistently nullary at all five sites ("nullary — the name is the fact"); the shell-injection exemption wording agrees verbatim across design D4, ADR-0013 §5, the spec-delta scenario, and the `refuse.rs:1745` test doc.
- `[docs/adr/0013:317-328, 331-340, 393-417]` — **Alignment** — Exactly one `*vocabulary-v2 revision.*` amendment recording all four moves (deleted pair + why, parity trio + D11 posture, `Referrer` payload, `TenantRef` sum with parse-time default) per D6; §2 gains `Restricted { pool }` only where pool already appeared (lines 68-84).
- `[docs/adr/0002:76-92]` — **Alignment** (modulo F4) — One amendment; wording matches D6 including structure-not-spelling and the `Ref…`/`M…` prefix-artifact permission.
- `[models/COVERAGE.md:224-256]` — **Alignment** — Intent-side counts match the grounding facts: 25-variant set, LinkSelfLoop 979/3000, RenameDoubleClaim and KernelDeclared recorded-unknowns pinned to directed tests — the known asymmetry is on record, not a defect; `alphabet.qnt:314-318` carries the matching honesty comment.
- `[docs/ROADMAP.md:23]` — **Alignment** — Row 3 records the vocabulary-v2 hardening with all four moves, dated 2026-09-07.
- `[crates/dpaa2-api/src/derive.rs:572-574 + crates/dpaa2-tools/src/main.rs:269-271 (+ models/intent/derive.qnt:589-591)]` — **Alignment** — 093.8 split-kernel-materialisation note present at both twins (shell port-only / core link-triggered), each naming the other.
- `[models/intent/refuse.qnt:44-50 vs crates/dpaa2-api/src/refuse.rs:50-63; models/intent/types.qnt:28-51 vs crates/dpaa2-api/src/intent.rs:49-50]` — **Alignment** — `Referrer` isomorphic case-for-case with D3's exact payloads; `TenantRef` model-authored, no `Default` derive; `intent_raw.qnt` DEVIATION updated (effTenant sentence gone, D1 two-key deviation present); `Refusal` `#[non_exhaustive]` with `PoolShortfall` reserved per the spec delta.
- **No scope creep** — every shipped move traces to D1–D6 or the recorded discovered decisions 093.7/.8; nothing landed outside the proposal's What Changes.

## Footer

**Read:** `openspec/changes/vocabulary-v2/{proposal,design,tasks}.md`, both spec deltas, `review/pass1.md`; `docs/adr/0013` §5 + §2 pool sites + line 462 target, `docs/adr/0002` §3 amendment; `models/COVERAGE.md` intent+raw sections; `docs/ROADMAP.md:23`; `CHANGELOG.md`; `package.json`; `models/intent/{types,refuse (Referrer + rule sites),alphabet (witness block),raw_alphabet (witness block),observed (honesty block),intent_raw (DEVIATION greps)}.qnt`, `scenarios/vwire.qnt`; `crates/dpaa2-api/src/{matcher.rs facet+test notes, refuse.rs Referrer/non_exhaustive/KernelDeclared sites, intent.rs TenantRef derive}`; `crates/dpaa2-verify/src/{intent_itf.rs decoder, ledger.rs R11 mechanism}`; commit subjects via `.git/logs/HEAD` (no shell available).

**Deliberately not read:** anything Pass 1's category-(b)/(c) table already classified (raw-side refusal encoding, TOML surface, tombstone prose); `derive.rs`/`main.qnt` bodies beyond the doc-note sites (model/Rust isomorphism internals belong to the other pass); `.beads/` beyond grep hits; nothing executed (read-only — R11/R14, `raw_conformance`, and the model ladder trusted to the recorded green floor).

**Open questions:**
1. PASS3-F3 caveat: without a diff I cannot prove which of the five unrecorded raw witnesses are new in this change vs pre-existing from task 3.3e; `wKernelDeclared` (a D4-coined name in a task-4.1-touched `package.json`) is almost certainly new, the four `wRaw*Present` may predate. The finding stands either way — the wired command reports counts the authoritative doc never records.
2. PASS3-F1's cheapest fix (reword 42dfe06/a50f412 to `!`) rebases five published-to-main descendants; if the series is considered sealed, the alternative is a design.md Migration Plan amendment plus a hand-noted CHANGELOG line — which contradicts "entirely managed with cliff". Caller's call.
