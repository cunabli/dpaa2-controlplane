// GENERATED by models/helpers/intent-inventory.py from
// models/board/baselines/reference.json — never hand-edited; a rerun is
// byte-identical. The reference board's inventory (intent-layer design D2):
// dpmac attributes as `dpmac info` reports them, availability from the
// ADR-0003 §3 safety matrix, DPL-owned objects per ADR-0001 §4, ceilings per
// ADR-0011 (the pool counts are the snapshot's `--resources` listing).
//
// Named invariants: none.
// Apalache-marked subset: none.
module intent_inventory {
  import types.* from "../core/types"
  import intent_types.* from "./types"

  pure val REF_INVENTORY: Inventory = {
    cpus: 16,  // ADR-0012: 16 online CPUs in the kernel container
    dpmacs: Map(
      3 -> { id: 3, maxRate: 25000, ethIf: CAUI, linkType: LinkPhy, avail: Reserved("ADR-0003 §3: wired to a peer that must never see traffic (total-deny)") },
      4 -> { id: 4, maxRate: 25000, ethIf: CAUI, linkType: LinkPhy, avail: Free },
      5 -> { id: 5, maxRate: 25000, ethIf: CAUI, linkType: LinkPhy, avail: Free },
      6 -> { id: 6, maxRate: 25000, ethIf: CAUI, linkType: LinkPhy, avail: Free },
      7 -> { id: 7, maxRate: 10000, ethIf: XFI, linkType: LinkPhy, avail: Free },
      8 -> { id: 8, maxRate: 10000, ethIf: XFI, linkType: LinkPhy, avail: Free },
      9 -> { id: 9, maxRate: 10000, ethIf: XFI, linkType: LinkPhy, avail: Free },
      10 -> { id: 10, maxRate: 10000, ethIf: XFI, linkType: LinkPhy, avail: Free },
      17 -> { id: 17, maxRate: 1000, ethIf: RGMII, linkType: LinkPhy, avail: Reserved("ADR-0003 §3: management plane (dpni.0), never touched") },
    ),
    foreign: Map({ fam: Dpni, num: 0 } -> "dpl"),  // ADR-0001 §4
    ceilings: Map(
      Dprc -> Unknown,  // ADR-0011: never measured
      Dpni -> Observed({ n: 18, provenance: "ADR-0011 decision 2 (V-CEIL-1, 2026-08-29): the 18th dpni is refused with every listed pool showing room; an unlisted resource" }),  // ADR-0011 decision 2: an unlisted resource, measured
      Dpbp -> Counted(63),  // ADR-0011 decision 1: the listed buffer-pool count (bp) is the ceiling
      Dpio -> Unknown,  // ADR-0011: the swp pool is listed but its gating was never measured — a recorded unknown
      Dpcon -> Unknown,  // ADR-0011: 64 created without a refusal (cap ended)
      Dpmcp -> Observed({ n: 203, provenance: "ADR-0011 decision 3 (V-CEIL-1 rev 2, 2026-08-29): MC portals are a fixed per-boot budget the listing reports, never returned by destroy" }),  // ADR-0011 decision 3: a per-boot budget, listed but never returned
      Dpseci -> Unknown,  // ADR-0011: never measured
      Dpsw -> Unknown,  // ADR-0011: never measured
    ),
  }
}
