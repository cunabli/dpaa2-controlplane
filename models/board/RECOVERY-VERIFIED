Recovery guarantee verified 2026-08-23 (task 5.1, board milestone).

Suite V-RECOVERY-1 applied the scratch mutation set (scratch container
under the Linux root, one dpbp created inside it, plugged), the operator
rebooted, and the post-boot diff against the pre-mutation capture came
back clean on all three views (generate-dpl reconstruction, dprc list,
root-container show). All three mutation steps also passed offline
read-back conformance. Evidence taken against the pinned reference pair
(MC 10.39.0, kernel 6.6.52); result files are operator material and are
not committed.

ADR-0003 §7's assumption — a reboot restores the DPL baseline — is now
verified. The presence of this file is what unblocks the generation of
mutating suites (dpaa2-verify's recovery gate).

Restore direction verified 2026-08-24 (task 5.4, suite V-DPRTC-3):
V-RECOVERY-1 proved a reboot erases additions; V-DPRTC-3 destroyed the
DPL-born dprtc.0 (after a driver unbind) and the reboot re-created it —
object, kernel driver binding and PTP chardev all back at the baseline
census. The guarantee holds in both directions.
